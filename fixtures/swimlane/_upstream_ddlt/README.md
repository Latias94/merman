# Mermaid 11.16 Swimlane DDLT Sources

These source fixtures are copied verbatim from:

`repo-ref/mermaid/cypress/platform/dev-diagrams/layout-tests/swimlanes`

The pinned source authority is Mermaid `11.16.0`. Generated size captures are intentionally not
copied: Merman measures labels through its render environment and uses these files to exercise the
complete parser, typed layout, routing, and post-processing chain.
