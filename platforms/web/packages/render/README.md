# @mermanjs/web-render

This is a private browser-only SVG rendering candidate. It is not published or supported as an
installation target until its independent workflow, package-size, and lifecycle evidence pass the
admission contract.

Use `@mermanjs/web` for production browser rendering today.
