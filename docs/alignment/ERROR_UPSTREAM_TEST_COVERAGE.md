# Error Upstream Test Coverage (Mermaid@11.12.2)

This document tracks which upstream Mermaid `error` diagram behaviors are covered in `merman` via:

- snapshot fixtures under `fixtures/error/*.mmd` (goldens generated by `cargo run -p xtask -- update-snapshots`)

Scope: Mermaid tag `@11.12.2`.

## Diagram registration

Source: `repo-ref/mermaid/packages/mermaid/src/diagram-api/diagram-orchestration.ts`

- `error` is a registered diagram type (a no-op parser in upstream): `fixtures/error/basic.mmd`
