# AGP-020 Journal

Date: 2026-05-29
Task: AGP-020
Status: Complete

## Change

- Confirmed comment handling and explicit-label precedence already matched copied fixtures after
  previous routing work.
- Fixed same-row reverse edges into a node with a self-loop so the reverse edge reuses the self-loop
  junction and the loop return lane drops by one row when needed.
- Moved parser/order target fixtures into the exact allowlist.

## Validation

- PASS `cargo fmt --all --check`
- PASS `cargo nextest run -p merman-ascii graph_fixture`
- PASS `cargo nextest run -p merman-ascii flowchart`
