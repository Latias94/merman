# WWP-080 Local Render Bench Panel

Added a local browser render bench surface to the playground.

Changes:

- Added a toolbar `Bench` dialog.
- Added warmup and measurement loops for Merman WASM and Mermaid JS.
- Added results for samples, median, p95, mean, min, max, errors, and Mermaid/Merman ratio.
- Added `@radix-ui/react-checkbox` for the existing shadcn checkbox component.
- Removed GPU-promoting SVG preview transforms that could make zoomed SVG text appear blurry.

Validation:

```bash
npm run build --prefix playground
```

Browser smoke:

- Loaded `http://127.0.0.1:5173/`.
- Opened `Bench`.
- Ran warmup `1`, measure `5`.
- Confirmed Merman and Mermaid JS result rows.
- Confirmed no preview `translate3d` or `will-change-transform` path remains.
- Screenshot: `target/playground-bench-smoke.png`.

Result: passed. The build still reports the existing large chunk warning.
