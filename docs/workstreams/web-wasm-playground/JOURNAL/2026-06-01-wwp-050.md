# WWP-050 GitHub Pages Build

Status: DONE
Date: 2026-06-01

## Scope

- Added `.github/workflows/pages.yml` for GitHub Pages builds and deployments.
- Added `playground/scripts/verify-dist-wasm.mjs`.
- Wired the verifier into `playground` as both `postbuild` and `verify:dist`.

## Validation

Positive gate:

```bash
npm ci --prefix platforms/web
npm run build --prefix platforms/web
npm run prepack --prefix platforms/web
npm ci --prefix playground
npm run build --prefix playground
npm run verify:dist --prefix playground
```

Result: passed. `playground/dist` contained `assets/merman_wasm_bg-BccmGt3e.wasm` and
`assets/merman_wasm-CW0mGF3B.js`.

Negative gate:

- Temporarily moved the generated `.wasm` out of `playground/dist/assets`.
- Ran `npm run verify:dist --prefix playground`.
- Restored the `.wasm` file.

Result: verifier failed as expected with exit code 1 when WASM was absent.

## Notes

- The first local full gate hit a Windows file-lock error during `npm ci --prefix playground`
  because the local Vite preview process was still running. After stopping that preview, the same
  workflow-equivalent command passed.
- GitHub repository settings may still need Pages source set to GitHub Actions.
