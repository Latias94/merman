# WRV-010 Journal

Date: 2026-05-30

## Work Completed

- Opened `workspace-release-versioning`.
- Recorded current package blockers for binding crates.
- Captured package evidence for `merman-render`, `merman-bindings-core`, and `merman-ffi`.

## Validation

- `cargo nextest run -p merman-ffi`
- `cargo package -p merman-ffi --allow-dirty --list`
- `cargo package -p merman-ffi --allow-dirty` failed as expected because `merman-bindings-core`
  is not published.
- `cargo package -p merman-bindings-core --allow-dirty` failed as expected because crates.io
  `merman-render 0.6.0` does not expose `ratex-math`.
- `cargo package -p merman-render --allow-dirty --list`
- `cargo package -p merman-render --allow-dirty`
- `cargo package -p merman-bindings-core --allow-dirty --list`

## Notes

The next task should choose a release version newer than `0.6.0` and document publish order before
changing manifests.
