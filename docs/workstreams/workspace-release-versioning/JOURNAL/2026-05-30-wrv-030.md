# WRV-030 Journal

Date: 2026-05-30

## Work Completed

- Updated `[workspace.package].version` to `0.7.0`.
- Updated internal workspace dependency requirements to `0.7.0`.
- Updated explicit `merman` and `merman-render` dependency requirements in
  `crates/merman-bindings-core/Cargo.toml`.
- Updated `docs/release/PUBLISH_ORDER.md` and workstream evidence.

## Validation

- `cargo check -p merman-ffi`
- `cargo check -p merman-uniffi`
- `rg '0\.6\.0' Cargo.toml Cargo.lock crates -g Cargo.toml`

## Notes

No feature gates were weakened. Package verification remains a separate WRV-040 task because
crates.io dependency availability changes step-by-step after each publish.
