# PUP-020 Through PUP-040 - Package Scaffold, Smoke, Closeout

Implemented:

- Added `platforms/python/merman` with `pyproject.toml`, README, package shim, and generated
  artifact ignore rules.
- Added `crates/merman-uniffi/examples/generate_python_package.rs`.
- Extended `bindgen_smoke.rs` with a staged Python package smoke that imports `merman` and
  calls `MermanEngine.render_svg` and `MermanEngine.parse_json`.
- Added `docs/bindings/PYTHON_UNIFFI.md` and linked it from `docs/bindings/UNIFFI.md`.

Validation:

- `cargo check -p merman-uniffi --features bindgen-smoke --examples` passed.
- `cargo nextest run -p merman-uniffi --features bindgen-smoke --test bindgen_smoke` passed with
  `2` tests.
- `cargo run -p merman-uniffi --features bindgen-smoke --example generate_python_package -- --package-dir <temp>`
  passed and populated a temporary package directory.
- `cargo fmt -p merman-uniffi -- --check` passed.
- `cargo clippy -p merman-uniffi --features bindgen-smoke --all-targets -- -D warnings` passed.
- `git diff --check` passed.
- `cargo package -p merman-uniffi --allow-dirty --list` passed and includes the generator example.

Closeout:

- Workstream closed.
- Wheel publishing and platform package matrices remain follow-on lanes.
