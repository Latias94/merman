# M07A-020 — Canonical Headless Render Operation Proof

Status: Done
Date: 2026-06-06

## Summary

Added `crates/merman/src/render/operation.rs` with a private `HeadlessRenderOperation` module.
`render_svg_sync` and `render_svg_with_pipeline_sync` now share parse, typed render model, layout,
SVG emission, postprocess metadata, and pipeline ordering through that module.

No public methods were deleted or renamed.

## Evidence

Passed:

- `cargo test -p merman --no-run`
- `cargo nextest run -p merman-bindings-core render_svg`
- `cargo nextest run -p merman-ffi render_svg`
- `cargo fmt --all --check`

Not counted as pass evidence:

- `cargo nextest run -p merman render_svg`
- `cargo nextest run -p merman render::svg_pipeline_tests`

Both `merman` nextest filters matched zero tests in the current package.

## Handoff

M07A-030 is now ready. Start by checking whether CLI/raster postprocessing should call a canonical
operation with metadata or remain an Adapter-owned format concern.
