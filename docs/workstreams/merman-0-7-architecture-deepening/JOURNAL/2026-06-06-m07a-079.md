# M07A-079 — Flowchart Spacing Zero Parity

Date: 2026-06-06

## Summary

Aligned Flowchart layout spacing with pinned Mermaid source:

- `nodeSpacing=0` now falls back to Mermaid's default `50`.
- `rankSpacing=0` now falls back to Mermaid's default `50`.
- `diagramPadding=0` remains an explicit preserved value in SVG viewBox rendering.

## Source Evidence

`repo-ref/mermaid/packages/mermaid/src/diagrams/flowchart/flowRenderer-v3-unified.ts` uses
`conf?.nodeSpacing || 50` and `conf?.rankSpacing || 50`, while `diagramPadding` uses `?? 8`.

## Validation

- RED: the two new spacing tests first failed with local zero spacing reducing node/rank distances.
- GREEN: `cargo nextest run -p merman-render flowchart_node_spacing_zero_falls_back_to_mermaid_default flowchart_rank_spacing_zero_falls_back_to_mermaid_default flowchart_diagram_padding_zero_is_preserved`
- GREEN: `cargo nextest run -p merman-render --test flowchart_layout_test --test flowchart_svg_test`

## Notes

This is a Flowchart-only source-backed fix. It intentionally does not add global numeric clamping
or change other diagram families.
