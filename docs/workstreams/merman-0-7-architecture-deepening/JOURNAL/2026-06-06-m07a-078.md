# M07A-078 Journal

Date: 2026-06-06
Status: Done

## Summary

- Added `HeadlessRenderer::with_fixed_today`,
  `HeadlessRenderer::with_fixed_local_offset_minutes`, and matching
  `HeadlessAsciiRenderer` builder methods.
- Added top-level binding `options_json.fixed_today` and
  `options_json.fixed_local_offset_minutes` fields with binding-level validation.
- Routed the fixed-time values into bindings-core renderer construction, so stateless and cached
  parse/layout/render/validation paths share the same deterministic Gantt time context.
- Updated the shared options JSON documentation and Web TypeScript options type.

## Validation

- `cargo nextest run -p merman-bindings-core parse_json_accepts_fixed_time_options` failed before
  implementation as RED evidence.
- `cargo nextest run -p merman-bindings-core`
- `cargo nextest run -p merman-bindings-core --features ascii`
- `cargo nextest run -p merman --features render headless_renderer_fixed_time_controls_semantic_parse`
- `cargo nextest run -p merman --features ascii headless_ascii_renderer_fixed_time_controls_semantic_parse`
- `npm run build:ts --prefix platforms/web`
- `cargo run -p xtask -- check-alignment`
- `cargo fmt --all --check`
- `git diff --check`
- `jq -c . docs/workstreams/merman-0-7-architecture-deepening/TASKS.jsonl >/dev/null`
- `jq -e . docs/workstreams/merman-0-7-architecture-deepening/WORKSTREAM.json >/dev/null`

## Remaining Risks

- Higher-level typed option builders for each platform package remain a convenience follow-on. The
  low-level contract intentionally stays JSON-based per ADR 0066.
- This slice does not alter Gantt parser, layout, theme, or SVG parity semantics; it only exposes
  the existing fixed-time context through more host adapter surfaces.
