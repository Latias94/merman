# M07A-040 — Pre-1.0 Public Adapter Surface Slimming

Status: Done
Date: 2026-06-06

## Summary

Removed shallow `HeadlessRenderer` convenience methods that only cloned `SvgRenderOptions` to set a
per-call `diagram_id` or passed one-shot `SvgRenderOptions` through existing helpers:

- `render_svg_readable_sync_with_diagram_id`
- `render_svg_resvg_safe_sync_with_diagram_id`
- `render_svg_sync_with`
- `render_svg_with_pipeline_sync_with`
- `render_svg_sync_with_diagram_id`
- `render_svg_with_pipeline_sync_with_diagram_id`
- `render_png_sync_with_diagram_id`
- `render_jpeg_sync_with_diagram_id`
- `render_pdf_sync_with_diagram_id`

Kept documented preset helpers:

- `render_svg_readable_sync`
- `render_svg_resvg_safe_sync`

No ADR was needed because repo/docs search found no callers and no README/bindings docs commitment
for the removed methods.

## Evidence

Passed:

- `cargo test -p merman --no-run`
- `cargo nextest run -p merman-bindings-core`
- `cargo nextest run -p merman-ffi`
- `cargo fmt --all --check`

Not counted as pass evidence:

- `cargo nextest run -p merman`

The `merman` package still matched zero nextest tests in the current package.

## Handoff

M07A-C1 is complete. The next campaign should be M07A-C2: Diagram Family Facts and Admission
Inventory, starting with M07A-050 after planner approval.
