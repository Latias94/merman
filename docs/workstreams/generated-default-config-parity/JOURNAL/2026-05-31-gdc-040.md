# 2026-05-31 - GDC-040

## Task

Clarify DOMPurify source checkout policy and update the baseline for Mermaid 11.15.

## Changes

- Updated DOMPurify source lock to `3.4.0`, commit
  `5b16e0b892e82b1779d62b9928b43c4c4ff290b9`.
- Materialized local `repo-ref/dompurify` at the locked tag for generation and verification.
- Regenerated `crates/merman-core/src/generated/dompurify_defaults.rs`.
- Updated generated artifact header to `DOMPurify 3.4.0`.
- Added actionable missing-reference remediation when default DOMPurify source material is absent.
- Kept DOMPurify inside the umbrella `verify-generated` gate.
- Updated ADR-0024.

## Evidence

- `cargo run -p xtask -- gen-dompurify-defaults` passed.
- `cargo run -p xtask -- verify-dompurify-defaults` passed.
- `cargo run -p xtask -- verify-generated` passed.
- `cargo nextest run -p xtask dompurify` passed, 2 tests.
- `cargo nextest run -p xtask` passed, 66 tests.
- `cargo nextest run -p merman-core sanitize` passed, 23 tests.
- `cargo nextest run -p merman-core` passed, 536 tests.
- `cargo fmt --check` passed.
- `git diff --check` passed.

## Follow-Up

GDC-050 can close this lane after review. The main follow-on candidate is Pie 11.15 config knobs
(`donutHole`, `highlightSlice`, `legendPosition`), which remain intentionally excluded from the
default-config override manifest until implemented.
