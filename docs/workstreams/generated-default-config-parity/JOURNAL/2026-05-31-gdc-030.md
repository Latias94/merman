# 2026-05-31 - GDC-030

## Task

Make `verify-default-config` green for the Mermaid 11.15 baseline.

## Changes

- Added `crates/xtask/default_config_overrides.json`.
- Made `gen-default-config` apply the override manifest by default.
- Added `--no-local-overrides` for schema-only diagnostics.
- Added xtask unit tests for set/remove override behavior.
- Updated ADR-0019 and workstream docs to describe the manifest contract.

## Manifest Policy

The manifest currently records:

- local parity overrides for `class.padding` and `flowchart.htmlLabels`,
- a JSON replacement for upstream `sankey.nodeColors: undefined`,
- removals for deferred/out-of-scope 11.15 diagram families,
- removals for Pie 11.15 config knobs whose rendering behavior is not implemented yet.

## Evidence

- `cargo nextest run -p xtask default_config` passed, 2 tests.
- `cargo run -p xtask -- verify-default-config` passed.
- `cargo run -p xtask -- gen-default-config --no-local-overrides --out target/xtask/default_config.schema_only.json` passed.
- `cargo nextest run -p xtask` passed, 64 tests.
- `cargo nextest run -p merman-core config` passed, 11 tests.
- `cargo nextest run -p merman-render` passed, 248 tests.
- `cargo fmt --check` passed.
- `git diff --check` passed.
- `cargo run -p xtask -- verify-generated` now fails only on missing
  `repo-ref/dompurify/dist/purify.cjs.js`.

## Follow-Up

GDC-040 should decide the DOMPurify reference checkout policy. The default-config gate is no longer
blocking the generated-artifact umbrella command.
