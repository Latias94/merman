# 2026-05-31 - GDC-020

## Task

Split generated artifact verification into artifact-specific xtask commands.

## Changes

- Added `verify-default-config`.
- Added `verify-dompurify-defaults`.
- Kept `verify-generated` as an umbrella command.
- Factored generated artifact checks into narrow helpers.
- Made umbrella failures aggregate across artifact families so one missing optional checkout does
  not hide a separate mismatch.
- Updated ADRs and rendering refactor docs to name the split gates.

## Evidence

- Red check before implementation: `cargo run -p xtask -- verify-default-config` failed with
  `UnknownCommand("verify-default-config")`.
- `cargo nextest run -p xtask` passed, 62 tests.
- `cargo run -p xtask -- verify-default-config` now fails only with `default config mismatch`.
- `cargo run -p xtask -- verify-dompurify-defaults` now fails only because
  `repo-ref/dompurify/dist/purify.cjs.js` is missing.
- `cargo run -p xtask -- verify-generated` reports both the default config mismatch and the
  DOMPurify missing-checkout error.
- `cargo fmt --check` passed.
- `git diff --check` passed.

## Follow-Up

GDC-030 should make `verify-default-config` green by modeling Mermaid 11.15 `defaultConfig.ts`
overlay semantics or by introducing an explicit generated override manifest.
