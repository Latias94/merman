# FRH-020..040 Journal

Completed the release-hardening pass for the C ABI.

Implementation:

- Added `crates/merman-ffi/tests/c_consumer_smoke.c`.
- Added `crates/merman-ffi/tests/c_consumer_smoke.rs`, which compiles the C source into a dynamic
  library, loads it, and calls the ABI through C function pointers.
- Added `crates/merman-ffi/README.md` and switched the package README to that file.
- Linked root README and `docs/bindings/FFI_PROTOCOL.md` to the C ABI entry point.
- Changed `merman-ffi`'s `ratex-math` feature to depend directly on `merman-render/ratex-math`
  instead of the higher-level `merman/ratex-math` re-export.

Validation:

- `cargo nextest run -p merman-ffi c_consumer_smoke` passed.
- `cargo nextest run -p merman-ffi` passed.
- `cargo nextest run -p merman-ffi --features ratex-math` passed.
- `cargo fmt -p merman-ffi -- --check` passed.
- `cargo clippy -p merman-ffi --all-targets -- -D warnings` passed.
- `cargo clippy -p merman-ffi --features ratex-math --all-targets -- -D warnings` passed.
- `cargo package -p merman-ffi --allow-dirty --list` passed.

Concern:

Full `cargo package -p merman-ffi --allow-dirty` fails because crates.io `merman-render 0.6.0` does
not expose the local `ratex-math` feature. This is a workspace release-version/publish-order issue.
