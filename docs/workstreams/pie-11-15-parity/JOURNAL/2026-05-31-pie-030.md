# PIE-030 Session

Date: 2026-05-31
Task: PIE-030
Status: DONE

## Summary

Restored Mermaid 11.15 Pie config keys in generated defaults and made the default-config generator
emit stable, recursively sorted JSON with a trailing newline.

## Changes

- Removed `pie.donutHole`, `pie.highlightSlice`, and `pie.legendPosition` removals from
  `crates/xtask/default_config_overrides.json`.
- Regenerated `crates/merman-core/src/generated/default_config.json` with those Pie defaults.
- Added a core regression test covering Pie default config values and directive overrides.
- Stabilized `gen-default-config` output ordering so rerunning the generator does not create broad
  insertion-order diffs.

## Evidence

- Red: `cargo nextest run -p merman-core parse_pie_exposes_11_15_config_defaults_and_overrides`
  failed because `pie.donutHole` was absent.
- Green: `cargo run -p xtask -- gen-default-config`
- Green: `cargo nextest run -p merman-core parse_pie_exposes_11_15_config_defaults_and_overrides`
- Green: `cargo nextest run -p xtask default_config`
- Green: `cargo run -p xtask -- verify-default-config`
- Green: `cargo nextest run -p merman-core config`
- Green: `cargo fmt --check`
- Green: `git diff --check`

## Next

PIE-040 should consume `pie.textPosition` and `pie.donutHole` in Pie layout/SVG geometry.
