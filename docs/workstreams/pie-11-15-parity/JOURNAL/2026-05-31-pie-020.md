# PIE-020 Session

Date: 2026-05-31
Task: PIE-020
Status: DONE

## Summary

Aligned baseline Pie slice layout with Mermaid 11.15 by preserving visible slice input order and
keeping hidden-slice color-domain reservation compatible with upstream.

## Changes

- Added renderer tests for input-order slices and hidden-slice color-domain slots.
- Removed local descending value sorting from Pie layout.
- Seeded the Pie color scale from all sections before filtering hidden `<1%` slices.
- Refreshed affected Pie layout goldens and the two affected upstream SVG baselines.

## Evidence

- Red: `cargo nextest run -p merman-render pie_slices_follow_input_order_in_mermaid_11_15`
  failed with old descending-value order.
- Red: `cargo nextest run -p merman-render pie_hidden_slices_still_reserve_color_domain_slots`
  failed because hidden slices did not reserve color-domain slots.
- Green: `cargo nextest run -p merman-render pie`
- Green: `cargo run -p xtask -- compare-pie-svgs --check-dom --dom-mode parity --dom-decimals 3`
- Green: `cargo run -p xtask -- compare-pie-svgs --check-dom --dom-mode parity-root --dom-decimals 3`
- Green: `cargo nextest run -p merman-render`
- Green: `cargo nextest run -p merman-core pie`
- Green: `cargo fmt --check`
- Green: `git diff --check`

## Next

PIE-030 should restore the Pie 11.15 config keys in the generated default config once the baseline
ordering fix is committed.
