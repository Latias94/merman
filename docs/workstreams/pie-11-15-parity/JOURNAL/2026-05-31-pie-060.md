# PIE-060 Session

Date: 2026-05-31
Task: PIE-060
Status: DONE

## Summary

Implemented Mermaid 11.15 Pie `highlightSlice` class and CSS behavior.

## Changes

- Added Pie highlight CSS rules for direct highlight and hover highlight.
- Emitted `pieCircle highlighted` when `pie.highlightSlice` matches a slice label.
- Emitted `pieCircle highlightedOnHover` when `pie.highlightSlice` is `hover`.
- Added SVG regression tests for direct and hover highlight behavior.

## Evidence

- Red: `cargo nextest run -p merman-render pie_highlight_slice_config_emits_highlight_classes_and_css`
  failed because direct highlight CSS/classes were absent.
- Red: `cargo nextest run -p merman-render pie_hover_highlight_slice_config_emits_hover_class_and_css`
  failed because hover highlight CSS/classes were absent.
- Green: `cargo nextest run -p merman-render pie_highlight_slice_config_emits_highlight_classes_and_css`
- Green: `cargo nextest run -p merman-render pie_hover_highlight_slice_config_emits_hover_class_and_css`
- Green: `cargo nextest run -p merman-render pie`
- Green: `cargo run -p xtask -- compare-pie-svgs --check-dom --dom-mode parity --dom-decimals 3`
- Green: `cargo run -p xtask -- compare-pie-svgs --check-dom --dom-mode parity-root --dom-decimals 3`
- Green: `cargo nextest run -p merman-render`
- Green: `cargo fmt --check`
- Green: `git diff --check`

## Next

PIE-070 should close the lane after final verification and any alignment-note update.
