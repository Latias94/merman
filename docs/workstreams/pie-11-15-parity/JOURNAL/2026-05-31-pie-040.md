# PIE-040 Session

Date: 2026-05-31
Task: PIE-040
Status: DONE

## Summary

Implemented Mermaid 11.15 `pie.textPosition` and `pie.donutHole` behavior in the Pie renderer.

## Changes

- Added Pie config helpers for `textPosition` and Mermaid's valid donut-hole range.
- Changed slice label layout to use configured `pie.textPosition`.
- Changed SVG path generation to render valid donut holes as annular sectors.
- Preserved upstream fallback behavior: invalid donut values render solid slices.
- Refreshed the three layout goldens that already configured non-default `textPosition`.

## Evidence

- Red: `cargo nextest run -p merman-render pie_text_position_config_moves_slice_labels`
  failed while labels still used the hardcoded `0.75` radius.
- Red: `cargo nextest run -p merman-render pie_donut_hole_config_renders_annular_slice_paths`
  failed while donut slices still closed through `L0,0Z`.
- Green: `cargo nextest run -p merman-render pie_text_position_config_moves_slice_labels`
- Green: `cargo nextest run -p merman-render pie_donut_hole_config_renders_annular_slice_paths`
- Green: `cargo nextest run -p merman-render pie`
- Green: `cargo run -p xtask -- compare-pie-svgs --check-dom --dom-mode parity --dom-decimals 3`
- Green: `cargo run -p xtask -- compare-pie-svgs --check-dom --dom-mode parity-root --dom-decimals 3`
- Green: `cargo nextest run -p merman-render`
- Green: `cargo fmt --check`
- Green: `git diff --check`

## Next

PIE-050 should implement `pie.legendPosition` transforms and root sizing.
