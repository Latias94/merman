# PIE-050 Session

Date: 2026-05-31
Task: PIE-050
Status: DONE

## Summary

Implemented Mermaid 11.15 Pie legend positioning for `top`, `bottom`, `left`, `right`, and
`center`.

## Changes

- Added Pie legend-position parsing and layout branching.
- Derived Pie bounds and legend anchor positions from the configured legend placement.
- Offset the pie group for `top` and `left` placements while keeping default/right output stable.
- Added layout and SVG regression tests for legend positioning.

## Evidence

- Red: `cargo nextest run -p merman-render pie_legend_position_config_controls_layout_regions`
  failed because layout bounds and legend positions were still default-right only.
- Red: `cargo nextest run -p merman-render pie_legend_position_top_and_left_move_the_pie_group`
  failed because the pie group was never offset for `top` or `left`.
- Green: `cargo nextest run -p merman-render pie_legend_position_config_controls_layout_regions`
- Green: `cargo nextest run -p merman-render pie_legend_position_top_and_left_move_the_pie_group`
- Green: `cargo nextest run -p merman-render pie`
- Green: `cargo run -p xtask -- compare-pie-svgs --check-dom --dom-mode parity --dom-decimals 3`
- Green: `cargo run -p xtask -- compare-pie-svgs --check-dom --dom-mode parity-root --dom-decimals 3`
- Green: `cargo nextest run -p merman-render`
- Green: `cargo fmt --check`
- Green: `git diff --check`

## Next

PIE-060 should add `pie.highlightSlice` classes and CSS while keeping empty/default output stable.
