# ACR-020 - Accept Color Role ADR

Date: 2026-05-30
Task: ACR-020
Status: DONE

## Summary

Accepted ADR 0067 for the ASCII color role API and moved the workstream from draft to active.

## Changes

- Added `docs/adr/0067-ascii-color-role-api.md`.
- Accepted a plain-by-default foreground color role API.
- Accepted one pre-1.0 `AsciiRenderOptions` migration: add color fields, keep `Copy`, add builder
  methods, and mark the struct `#[non_exhaustive]`.
- Updated the workstream handoff so ACR-030 is the next implementation task.

## Verification

- `git diff --check -- docs/adr/0067-ascii-color-role-api.md docs/workstreams/ascii-color-role-api` - PASS.
- `cargo fmt --all --check` - PASS.
