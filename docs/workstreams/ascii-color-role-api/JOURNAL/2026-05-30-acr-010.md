# ACR-010 - Draft Color Role API

Date: 2026-05-30
Task: ACR-010
Status: DONE

## Summary

Opened a draft workstream for an opt-in ASCII color role API.

## Changes

- Proposed plain-by-default `AsciiColorMode`.
- Proposed non-exhaustive semantic `AsciiColorRole`.
- Proposed private-field `AsciiColorTheme` with builder methods.
- Identified `AsciiRenderOptions` public-field migration as an ADR-level decision.
- Split implementation into role-aware canvas, flowchart vertical slice, broader family adoption,
  and Mermaid style mapping follow-ons.

## Verification

- `git diff --check -- docs/workstreams/ascii-color-role-api` - PASS.
- `cargo fmt --all --check` - PASS.
