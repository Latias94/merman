# AFDT-040 - Docs And Closeout

Date: 2026-05-30
Task: AFDT-040
Status: DONE

## Review

No blocking workstream-compliance or code-quality findings remain. AFDT-030 satisfied the core
target state with parser-backed BT/RL tests, readable mirrored labels, Unicode connector coverage,
and package/lint gates. AFDT-040 fixed the remaining stale public support docs.

## Changes

- Updated `crates/merman-ascii/FLOWCHART_SUPPORT.md` so BT/RL root directions are listed as
  supported instead of explicitly unsupported.
- Updated `crates/merman-ascii/README.md` and root `README.md` to mention flowchart
  LR/TD/TB/BT/RL text output support.
- Closed the workstream documents and recorded deferred follow-ons.

## Verification

- `cargo nextest run -p merman-ascii` - PASS
- `cargo clippy -p merman-ascii --all-targets -- -D warnings` - PASS
- `cargo fmt --all --check` - PASS
- `git diff --check` - PASS

## Follow-Ons

- Subgraph direction overrides from `FlowSubgraph.dir`.
- ANSI/HTML color roles and `classDef`/`class`/inline style rendering.
- State diagram graph rendering through a state-to-graph semantic adapter.
- Multiline subgraph labels.
- Additional uncommon flowchart shape families.
