# AFDT-020 Journal

Date: 2026-05-30

Added parser-backed flowchart direction contract tests:

- `flowchart_parser_bt_root_direction_renders_with_vertical_flip`
- `flowchart_parser_rl_root_direction_renders_with_horizontal_mirror`

Both tests currently fail red through public `render_model` behavior with
`non-LR/TD graph directions`. AFDT-030 should make these tests green and update the existing
unsupported-direction test, which still uses BT as the unsupported sample.
