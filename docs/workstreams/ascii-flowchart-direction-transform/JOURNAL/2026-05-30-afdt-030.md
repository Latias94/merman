# AFDT-030 - Root Direction Transform

Date: 2026-05-30
Task: AFDT-030
Status: DONE

## Summary

Implemented flowchart BT and RL root-direction rendering in `merman-ascii` without changing
`merman-core` or parser behavior. The graph renderer now keeps layout/routing canonical to TD/LR
and applies a final output transform for BottomTop and RightLeft.

## Changes

- Added `GraphDirection::BottomTop` and `GraphDirection::RightLeft`.
- Accepted `BT` and `RL` in the flowchart ASCII adapter.
- Canonicalized layout/routing to existing TD/LR implementations.
- Mirrored the final canvas vertically or horizontally and remapped ASCII/Unicode route characters.
- Redrew node labels, edge labels, and subgraph titles after transforms so text remains readable.
- Replaced the old parser-backed BT unsupported-direction test with direct-model unsupported
  direction coverage using `XX`.

## Verification

- `cargo nextest run -p merman-ascii flowchart_parser_rl_multi_character_node_labels_stay_readable flowchart_parser_rl_edge_labels_stay_readable` - PASS
- `cargo nextest run -p merman-ascii flowchart_parser_rl_chain_mirrors_unicode_connectors` - PASS
- `cargo nextest run -p merman-ascii flowchart` - PASS
- `cargo nextest run -p merman-ascii` - PASS
- `cargo clippy -p merman-ascii --all-targets -- -D warnings` - PASS
- `cargo fmt --all --check` - PASS
- `git diff --check` - PASS

## Next

AFDT-040 should update support documentation/readme references, run final closeout gates, and then
close or split any remaining flowchart direction work.
