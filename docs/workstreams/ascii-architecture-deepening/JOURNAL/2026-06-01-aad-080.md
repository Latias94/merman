# AAD-080 — Local Subgraph Direction Subset

Date: 2026-06-01
Owner: codex

## Summary

Resumed the previously closed `ascii-architecture-deepening` lane to land a bounded
`A-GRAPH-010` slice: local canonical `LR` subgraph direction inside canonical `TD` root flowcharts
when all routed edges remain internal to the subgraph.

## What Changed

- threaded `FlowSubgraph.dir` through the graph adapter into `AsciiGraphGroup`;
- taught graph placement to re-place eligible subgraph members using a local canonical direction;
- taught route selection and route extent calculation to inspect per-edge local group direction;
- kept cross-boundary mixed-direction cases on the existing global fallback path;
- updated flowchart support and gap registry text to describe the shipped subset honestly;
- added parser/model tests for the shipped subset and the deliberate cross-boundary fallback.

## Validation

- `cargo nextest run -p merman-ascii flowchart subgraph`
- `cargo nextest run -p merman-ascii graph_fixture`

## Notes

- This is intentionally not full Mermaid subgraph-direction parity.
- Remaining work is primarily cross-boundary mixed-direction routing, broader local `TD`
  combinations, and nested mixed-direction behavior.
