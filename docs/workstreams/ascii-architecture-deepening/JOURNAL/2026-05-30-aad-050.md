# 2026-05-30 — AAD-050 Sequence Event Plan Seam

Implemented the sequence event-plan slice:

- added `SequenceEventPlan` in `crates/merman-ascii/src/sequence/plan.rs`;
- moved activation count updates, actor visibility, lifecycle visibility transitions, and control
  frame ordering state out of the render loop;
- kept row painting in `render.rs`;
- added focused event-plan unit tests for activation counts, empty control sections, and lifecycle
  visibility/reset behavior.

Validation:

- `cargo nextest run -p merman-ascii event_plan`
- `cargo nextest run -p merman-ascii sequence`
- `cargo nextest run -p merman-ascii`
- `cargo fmt --all --check`
- `cargo clippy -p merman-ascii --all-targets -- -D warnings`
