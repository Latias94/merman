# 2026-05-30 — AAD-030 Graph Route Planning And Painting Seam

Implemented the first graph route-planning seam:

- added `crates/merman-ascii/src/graph/routing/plan.rs`;
- moved top-down direct route planning into a pure route plan that records cells and label anchors;
- kept painting in `routing.rs`, where planned cells are applied to `Canvas` and `RouteCells`;
- left the rest of graph routing on the existing drawing path for later expansion.

Validation:

- `cargo nextest run -p merman-ascii top_down_direct`
- `cargo nextest run -p merman-ascii flowchart`
- `cargo nextest run -p merman-ascii`
- `cargo fmt --all --check`
- `cargo clippy -p merman-ascii --all-targets -- -D warnings`
