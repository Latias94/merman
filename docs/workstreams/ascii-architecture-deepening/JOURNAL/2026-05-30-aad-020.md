# 2026-05-30 — AAD-020 Shared Styled Text/Cell Module

Implemented the first architecture slice:

- added `StyledCell` and `StyledLine` to `crates/merman-ascii/src/text.rs`;
- migrated sequence `SequenceLine` to a type alias over `StyledLine`;
- migrated XYChart `ChartLine` and `ChartCell` to the same substrate;
- kept relation graph lines unchanged for AAD-040, where the relation graph adapter surface will be
  deepened.

Validation:

- `cargo nextest run -p merman-ascii canvas color`
- `cargo nextest run -p merman-ascii text`
- `cargo nextest run -p merman-ascii sequence`
- `cargo nextest run -p merman-ascii xychart`
- `cargo nextest run -p merman-ascii`
- `cargo fmt --all --check`
- `cargo clippy -p merman-ascii --all-targets -- -D warnings`
