# 2026-05-30 — AAD-060 ASCII Gap Registry

Implemented the ASCII gap registry slice:

- added `crates/merman-ascii/ASCII_GAP_REGISTRY.md`;
- mapped remaining text, style, graph, sequence, class/ER, XYChart, and family-expansion gaps to
  owning modules, dependencies, validation gates, and support-doc sources;
- linked the registry from `crates/merman-ascii/README.md`.

Validation:

- `git diff --check -- crates/merman-ascii docs/workstreams/ascii-architecture-deepening`
