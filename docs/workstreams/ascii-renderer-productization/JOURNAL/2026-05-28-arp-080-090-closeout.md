# ARP-080 / ARP-090 CLI Support And Closeout

Date: 2026-05-28
Owner: codex
Status: DONE

## Work Completed

- Added `merman-cli` `ascii` feature forwarding to `merman/ascii`.
- Added `render --format ascii|unicode` output support.
- Preserved existing SVG/raster behavior and default output format.
- Added feature-gated CLI smoke tests for Unicode stdout and ASCII file output.
- Fixed CLI parsing so a positional `-` is accepted as documented stdin input.
- Updated root README, `crates/merman-cli/README.md`, and CHANGELOG.
- Closed the ASCII renderer productization workstream after final verification.

## Validation

- `cargo nextest run -p merman-cli --features ascii ascii` passed: 2 tests.
- `cargo nextest run -p merman-cli` passed: 8 tests.
- `cargo check -p merman-cli --features ascii` passed.
- `cargo nextest run -p merman-cli --features ascii` passed: 10 tests.
- `cargo clippy -p merman-cli --features ascii --all-targets -- -D warnings` passed.
- `cargo package -p merman-ascii --list --allow-dirty` passed.

Final closeout gates were rerun after documentation updates:

- `cargo fmt --all --check` passed.
- `cargo nextest run -p merman-ascii` passed: 37 tests.
- `cargo nextest run -p merman --features ascii` passed: 3 tests.
- `cargo nextest run -p merman-cli` passed: 8 tests.
- `cargo nextest run -p merman-cli --features ascii` passed: 10 tests.
- `cargo clippy -p merman-ascii -p merman --features ascii --all-targets -- -D warnings`
  passed.
- `cargo clippy -p merman-cli --features ascii --all-targets -- -D warnings` passed.
- `git diff --check` passed.

## Residual Risks

- Current graph/sequence support is intentionally subset-first. Unsupported Mermaid constructs are
  documented and should become future feature lanes rather than reopening this productization lane.
- Published crate size should be reviewed once compatibility stabilizes because copied fixture
  inventory is intentionally tracked for CI/source verification.
