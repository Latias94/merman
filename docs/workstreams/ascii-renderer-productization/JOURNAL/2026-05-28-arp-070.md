# ARP-070 Public API And Host Integration

Date: 2026-05-28
Owner: codex
Status: DONE

## Work Completed

- Added the top-level `merman` `ascii` feature and optional `merman-ascii` dependency.
- Added `merman::ascii` with public re-exports, `render_ascii_sync`, async `render_ascii`, and
  `HeadlessAsciiRenderer`.
- Added public API integration tests under `crates/merman/tests/ascii_api.rs`.
- Added the runnable `crates/merman/examples/ascii_output.rs` example with Unicode default output
  and a `--ascii` switch.
- Updated the root README with install, API, and example commands for ASCII/Unicode output.

## Validation

- `cargo run -p merman --features ascii --example ascii_output` passed.
- `cargo run -p merman --features ascii --example ascii_output -- --ascii` passed.
- `cargo check -p merman --features ascii` passed.
- `cargo nextest run -p merman-ascii` passed: 37 tests.
- `cargo nextest run -p merman --features ascii` passed: 3 tests.
- `cargo clippy -p merman-ascii -p merman --features ascii --all-targets -- -D warnings`
  passed.
- `cargo test -p merman --doc --features ascii` passed: 0 doctests.
- `cargo fmt --all --check` passed.
- `git diff --check` passed.

## Notes

CLI output integration remains intentionally separate. The next task should decide whether to ship
CLI `ascii`/`unicode` support inside this lane or split it into a narrower follow-on.

Next task: ARP-080 CLI support or explicit split.
