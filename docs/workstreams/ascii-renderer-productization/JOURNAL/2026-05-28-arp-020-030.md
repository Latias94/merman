# 2026-05-28 - ARP-020/ARP-030

Status: DONE

## Scope

- Added `crates/merman-ascii` as a workspace crate.
- Added public API foundation: `AsciiRenderer`, `AsciiRenderOptions`, `AsciiCharset`,
  `AsciiDirection`, `AsciiError`, and render entrypoints.
- Added explicit unsupported-feature gates for flowchart and sequence rendering while algorithms
  are not yet ported.
- Copied the upstream `mermaid-ascii` MIT license into tracked crate files.
- Copied upstream golden fixtures from `repo-ref/mermaid-ascii/cmd/testdata` into tracked
  `crates/merman-ascii/tests/testdata/mermaid-ascii`.
- Added fixture inventory tests for source provenance and expected fixture counts.

## Evidence

- `cargo fmt --all --check` passed.
- `cargo check -p merman-ascii` passed.
- `cargo nextest run -p merman-ascii` passed: 5 tests.
- `cargo nextest run -p merman-ascii fixture_inventory` passed: 2 tests.
- `cargo clippy -p merman-ascii --all-targets -- -D warnings` passed.
- `cargo test -p merman-ascii --doc` passed: 0 doctests.
- `cargo package -p merman-ascii --list --allow-dirty` passed and listed the upstream MIT license
  copy plus copied golden fixtures in the crate package.
- `git diff --check` passed.
- Added a scoped `.gitattributes` whitespace override for copied `mermaid-ascii` text goldens
  because several expected character-grid outputs intentionally preserve trailing cells.

## Notes

The fixture inventory tracks source commit `6fffb8e` from
`https://github.com/AlexanderGrooff/mermaid-ascii`. `repo-ref/` remains a research-only ignored
directory and is not required by the new tests.

Next task: ARP-040.
