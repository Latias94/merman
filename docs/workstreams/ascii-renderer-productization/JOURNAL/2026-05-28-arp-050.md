# 2026-05-28 - ARP-050

Status: DONE

## Scope

- Added parser/model-level flowchart tests that use
  `merman_core::Engine::parse_diagram_for_render_model_sync`.
- Verified basic `flowchart LR` and `flowchart TB` inputs render through public `render_model`.
- Compared parser-produced models against copied upstream `mermaid-ascii` graph goldens.
- Hardened the `FlowchartV2Model` adapter for real parser fields such as `layoutShape:
  squareRect`, `type: arrow_point`, and `stroke: normal`.
- Added explicit unsupported-feature coverage for edge labels, subgraphs, non-LR/TD directions,
  non-rectangular node shapes, dotted/non-normal edge strokes, and open/non-point arrows.
- Added explicit unsupported-feature coverage for hand-built models with missing edge endpoint
  nodes.
- Added `crates/merman-ascii/FLOWCHART_SUPPORT.md` and linked it from the crate README.

## Evidence

- `cargo nextest run -p merman-ascii flowchart` passed: 18 tests.
- `cargo check -p merman-ascii` passed.
- `cargo nextest run -p merman-ascii` passed: 28 tests.
- `cargo clippy -p merman-ascii --all-targets -- -D warnings` passed.
- `cargo test -p merman-ascii --doc` passed: 0 doctests.
- `cargo fmt --all --check` passed.
- `git diff --check` passed.

## Notes

The adapter still supports only the documented tracer-bullet subset. `FLOWCHART_SUPPORT.md` is the
authoritative current support matrix until broader routing, subgraphs, edge labels, and additional
shape semantics are implemented.

Next task: ARP-060.
