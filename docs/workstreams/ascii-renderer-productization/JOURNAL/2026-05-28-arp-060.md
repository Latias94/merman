# ARP-060 Sequence Vertical Slice

Date: 2026-05-28
Owner: codex
Status: DONE

## Work Completed

- Ported the initial `mermaid-ascii` sequence layout/drawing algorithm into
  `crates/merman-ascii/src/sequence.rs`.
- Routed `render_sequence` and `render_model` through the new sequence adapter.
- Added parser/model-level golden tests for copied upstream sequence fixtures:
  - 12 Unicode sequence fixtures.
  - 5 ASCII sequence fixtures.
- Added explicit unsupported-feature tests for non-basic sequence semantics, including titles,
  notes, activations, actor shapes, actor wrapping, actor links/properties, boxes,
  create/destroy, message placement, wrapped messages, control messages, unknown actors, and
  unsupported message types.
- Added `crates/merman-ascii/SEQUENCE_SUPPORT.md` and refreshed the crate README status.

## Validation

- `cargo fmt --all --check` passed.
- `cargo check -p merman-ascii` passed.
- `cargo nextest run -p merman-ascii sequence` passed: 10 tests.
- `cargo nextest run -p merman-ascii sequence_golden` passed: 2 tests.
- `cargo nextest run -p merman-ascii` passed: 37 tests.
- `cargo clippy -p merman-ascii --all-targets -- -D warnings` passed.
- `cargo test -p merman-ascii --doc` passed: 0 doctests.
- `git diff --check` passed.

## Notes

The copied upstream sequence fixture comparisons intentionally normalize trailing whitespace, which
matches `mermaid-ascii`'s own test behavior. This keeps character-grid trailing cells from becoming
release-significant while still proving visible output parity.

Next task: ARP-070 public API and host integration.
