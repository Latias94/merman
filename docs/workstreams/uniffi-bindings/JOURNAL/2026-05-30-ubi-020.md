# UBI-020 Journal

Completed the shared safe binding facade extraction.

Implementation:

- Added `crates/merman-bindings-core`.
- Moved options JSON parsing, source UTF-8 validation, renderer setup, SVG pipeline selection,
  RaTeX feature gating, result-code names, and JSON error payload serialization into the safe
  facade.
- Refactored `crates/merman-ffi` to delegate to the facade and keep only unsafe C boundary
  concerns: raw pointer validation, panic containment, owned buffer transfer, and exported symbols.
- Preserved public C function names and result-code values.

Validation:

- `cargo check -p merman-bindings-core`
- `cargo nextest run -p merman-bindings-core`
- `cargo nextest run -p merman-bindings-core --features ratex-math`
- `cargo nextest run -p merman-ffi`
- `cargo nextest run -p merman-ffi --features ratex-math`
- `cargo check -p merman-bindings-core --features raster,ratex-math`
- `cargo nextest run -p merman-ffi --features raster,ratex-math`
- `cargo clippy -p merman-bindings-core --all-targets -- -D warnings`
- `cargo clippy -p merman-bindings-core --features ratex-math --all-targets -- -D warnings`
- `cargo clippy -p merman-ffi --all-targets -- -D warnings`
- `cargo clippy -p merman-ffi --features ratex-math --all-targets -- -D warnings`
- `cargo fmt -p merman-bindings-core -p merman-ffi -- --check`
- `git diff --check`
