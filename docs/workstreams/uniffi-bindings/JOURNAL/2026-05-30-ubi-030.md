# UBI-030 Journal

Completed the minimal UniFFI crate.

Implementation:

- Added `crates/merman-uniffi`.
- Added a UniFFI `MermanEngine` object with `render_svg`, `parse_json`, and `layout_json`.
- Added `MermanError::Binding { code, code_name, message }` to preserve shared facade status
  details for generated bindings.
- Kept the crate over `merman-bindings-core`; no platform package wrappers were added.

Validation:

- `cargo check -p merman-uniffi`
- `cargo test -p merman-uniffi`
- `cargo nextest run -p merman-uniffi`
- `cargo check -p merman-uniffi --features ratex-math`
- `cargo check -p merman-uniffi --features raster,ratex-math`
- `cargo clippy -p merman-uniffi --all-targets -- -D warnings`
- `cargo clippy -p merman-uniffi --features ratex-math --all-targets -- -D warnings`
- `cargo fmt -p merman-uniffi -- --check`
- `git diff --check`
