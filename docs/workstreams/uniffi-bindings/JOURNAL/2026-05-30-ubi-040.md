# UBI-040 Journal

Date: 2026-05-30

## Work Completed

- Added `bindgen-smoke` to `crates/merman-uniffi`.
- Added `crates/merman-uniffi/tests/bindgen_smoke.rs`.
- Added `docs/bindings/UNIFFI.md`.
- Updated workstream evidence, milestones, TODO, handoff, and continue policy.

## Validation

- `cargo test -p merman-uniffi --features bindgen-smoke --test bindgen_smoke`
- `cargo check -p merman-uniffi --features bindgen-smoke`
- `cargo nextest run -p merman-uniffi --features bindgen-smoke --test bindgen_smoke`
- `cargo nextest run -p merman-uniffi`
- `cargo clippy -p merman-uniffi --features bindgen-smoke --all-targets -- -D warnings`

## Notes

The smoke test generates Python bindings into a temporary directory. Generated platform bindings are
not committed; iOS, Android, Python packaging, Flutter, Node, and raster outputs remain follow-on
lanes.
