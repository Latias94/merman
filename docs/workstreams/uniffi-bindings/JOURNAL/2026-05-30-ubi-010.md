# UBI-010 Journal

Opened the `uniffi-bindings` workstream.

Inputs:

- ADR 0066 keeps C ABI canonical and treats UniFFI as an optional high-level layer.
- `ffi-api` recommends extracting/formalizing a shared safe facade before adding UniFFI.
- `ffi-release-hardening` confirms the C ABI is hardened locally, with a separate package
  release-order concern.
- `cargo search uniffi --limit 3` and `cargo info uniffi@0.31.1` confirm UniFFI 0.31.1.

First executable task:

- `UBI-020`: create the safe shared facade and make `merman-ffi` delegate to it.
