# ACEPR-020 Parallel Contract Tests

Date: 2026-05-30
Owner: codex
Status: DONE

## Summary

Added parser-backed classDiagram and erDiagram tests for two relationships between the same
endpoints. The tests assert public `render_model` output instead of adapter internals.

## Evidence

- `cargo nextest run -p merman-ascii class_parser_parallel_relationship_layout_renders_each_lane`
  failed red on `parallel class relationship layouts`.
- `cargo nextest run -p merman-ascii er_parser_parallel_relationship_layout_renders_each_lane`
  failed red on `parallel ER relationship layouts`.

## Next

ACEPR-030 implements the narrow same-endpoint lane renderer and keeps broader dense routing out of
scope.
