# ACEPR-030 Shared Parallel Lane Helper

Date: 2026-05-30
Owner: codex
Status: DONE

## Summary

Added shared vertical lane formatting in `relation_graph` and routed class/ER same-endpoint
parallel relationships through it. Class and ER adapters still own marker, label, cardinality, and
line-style semantics.

The implementation deliberately activates only when all relationships in the component share the
same top/bottom endpoints. Mixed endpoint pairs with parallel edges remain broader dense-routing
work.

## Evidence

- `cargo nextest run -p merman-ascii class`: PASS.
- `cargo nextest run -p merman-ascii er`: PASS.
- `cargo clippy -p merman-ascii --all-targets -- -D warnings`: PASS.
- `cargo fmt --all --check`: PASS.
- `git diff --check -- crates/merman-ascii/src/relation_graph.rs crates/merman-ascii/src/class/render.rs crates/merman-ascii/src/er/render.rs crates/merman-ascii/tests/class_model.rs crates/merman-ascii/tests/er_model.rs docs/workstreams/ascii-class-er-parallel-routing`: PASS.

## Next

ACEPR-040 should update public support docs, run full package gates, and close the lane.
