# 2026-05-28 Closeout

## Completed

- Added `RenderSemanticModel::kind()` for canonical typed model names.
- Added `RenderSemanticModel::supports_diagram_type()` for alias compatibility.
- Replaced `Engine::render_model_kind` with model-owned metadata.
- Simplified `layout_parsed_render_layout_only` to validate compatibility once and match only on
  typed variants.
- Added focused core tests for kind and alias behavior.
- Added focused renderer tests for alias layout dispatch and mismatched typed-model rejection.

## Evidence

- `cargo nextest run -p merman-core render_semantic_model`: pass, 2 tests.
- `cargo nextest run -p merman-render render_model`: pass, 2 tests.
- `cargo nextest run -p merman-core`: pass, 523 tests.
- `cargo nextest run -p merman-render`: pass, 205 tests.
- `cargo fmt -p merman-core -p merman-render -- --check`: pass.
- `cargo clippy -p merman-core --all-targets -- -D warnings`: pass.
- `cargo clippy -p merman-render --all-targets -- -D warnings`: pass.

## Follow-On

Generated parse-render dispatch can be split into a separate lane if future diagram additions keep
creating boilerplate. Text measurement caching remains out of scope for this lane.
