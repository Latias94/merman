# TCD-040 Journal - Public Theme Renderability Coverage

Date: 2026-06-04
Status: DONE

## Scope

Verified public renderability/theme coverage for the render-side theme capability lane.

Reviewed:

- `crates/merman/tests/theme_renderability_smoke.rs`
- `docs/workstreams/headless-parity-deepening/THEME_RENDERING_COVERAGE.md`

## Decision

No new public test fixture was added.

The existing HPD-080 public smoke already exercises the relevant public API surface through
`HeadlessRenderer`:

- Flowchart, Class, State, Sequence, and Block visible theme signals cover the TCD-020
  `PresentationTheme` migration path.
- XyChart explicit `xyChart.plotColorPalette` visible colors cover the TCD-030 public path.

Adding another snapshot or duplicate smoke case would not strengthen the seam proof. The direct
`chart_palette` helper tests remain the correct place for the missing-palette derivation branch,
because normal public effective config already resolves Mermaid theme palettes before rendering.

## Validation

The planned command in the task ledger was audited first:

- `cargo test -p merman theme_renderability_smoke --features render` completed but ran `0` tests.

The real integration-test gate passed:

- `cargo test -p merman --features render --test theme_renderability_smoke` passed, `12` tests.

## Follow-Up

TCD-050 should review/close this lane or split residual raw-theme follow-ons. Bindings, playground
UX, and host-specific styling policy remain out of scope for this lane unless a separate follow-on
is opened.
