# ACECL-020 Component Contract Tests

Date: 2026-05-30
Owner: codex
Status: DONE

## Summary

Added parser-backed classDiagram and erDiagram tests for a renderable relationship plus an
unrelated standalone class/entity. The tests replace the old public unsupported-diagnostic
expectations.

## Evidence

- `cargo nextest run -p merman-ascii class_parser_relationship_layouts_render_unrelated_classes_as_components` failed red on the old class unsupported diagnostic.
- `cargo nextest run -p merman-ascii er_parser_relationship_layouts_render_unrelated_entities_as_components` failed red on the old ER unsupported diagnostic.

## Next

ACECL-030 implements shared component rendering while preserving existing chain, star, and crossing
behavior.
