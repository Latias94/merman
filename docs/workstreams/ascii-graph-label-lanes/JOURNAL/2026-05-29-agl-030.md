# AGL-030 Journal

Date: 2026-05-29
Task: AGL-030
Status: Complete

## Change

- Added routed edge label collection so labels are overlaid after all route cells, matching the Go
  renderer's draw order.
- Added LR parallel bottom lanes for duplicate and reverse same-row edge pairs.
- Added TD right-side back lanes with vertical label placement for reverse edges.
- Made LR and TD layout reserve label-driven spacing on the routed line instead of using a separate
  top label row.
- Moved these fixtures into exact graph parity:
  - `ascii/back_edges_two_labels_td.txt`
  - `ascii/bidirectional_edge_labels_lr.txt`
  - `ascii/bidirectional_edge_labels_td.txt`
  - `ascii/duplicate_edge_labels.txt`
  - `extended-chars/back_edges_two_labels_td.txt`

## Validation

- PASS `cargo fmt --all --check`
- PASS `cargo nextest run -p merman-ascii graph_fixture`
- PASS `cargo nextest run -p merman-ascii graph::`
- PASS `cargo nextest run -p merman-ascii flowchart`

## Notes

- Exact graph fixture parity is now 53: 32 ASCII and 21 Unicode.
- Remaining graph gaps are primarily comments, definition-order preservation, padding, multiline
  labels, and subgraph-heavy layouts.
