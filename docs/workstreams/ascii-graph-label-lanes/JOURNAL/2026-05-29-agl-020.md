# AGL-020 Journal

Date: 2026-05-29
Task: AGL-020
Status: Complete

## Change

- Added `crates/merman-ascii/src/graph/routing/path.rs`.
- Moved grid port selection, A* grid search, path merging, and step-direction helpers out of
  `routing.rs`.
- Kept the new path API private to the routing module with `pub(super)` visibility.

## Validation

- PASS `cargo fmt --all --check`
- PASS `cargo nextest run -p merman-ascii graph_fixture`
- PASS `cargo nextest run -p merman-ascii graph::`

## Notes

- No fixture parity change was intended in this task.
- AGL-030 should now add duplicate and bidirectional edge label lanes on top of the routed edge
  metadata instead of expanding the drawing module further.
