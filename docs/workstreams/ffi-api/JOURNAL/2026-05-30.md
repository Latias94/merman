# 2026-05-30

- Opened the FFI API workstream as draft.
- Added ADR 0066 to choose a stable C ABI as the canonical low-level boundary, with UniFFI as an
  optional high-level binding layer.
- Used `repo-ref/RaTeX` as the local reference for a small FFI crate, heap-owned JSON payloads,
  explicit free functions, and platform wrapper strategy.
