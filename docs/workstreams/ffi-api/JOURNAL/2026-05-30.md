# 2026-05-30

- Opened the FFI API workstream as draft.
- Added ADR 0066 to choose a stable C ABI as the canonical low-level boundary, with UniFFI as an
  optional high-level binding layer.
- Used `repo-ref/RaTeX` as the local reference for a small FFI crate, heap-owned JSON payloads,
  explicit free functions, and platform wrapper strategy.
- Completed `FFI-010` by freezing the first-slice protocol decisions in `DESIGN.md`: type names,
  first exported function names, result codes, error JSON payloads, buffer ownership, input rules,
  options JSON, and stateless threading.
- Completed `FFI-020` by adding `crates/merman-ffi`, workspace registration, `merman_render_svg`,
  `merman_buffer_free`, owned buffers, structured error JSON, options parsing, panic containment,
  and focused tests.
- Completed `FFI-030` by adding `crates/merman-ffi/include/merman.h`,
  `docs/bindings/FFI_PROTOCOL.md`, a `header_smoke` C compile test, and fresh focused gates.
- Completed `FFI-040` by adding `merman_parse_json`, `merman_layout_json`, header/protocol updates,
  and parse/layout ABI tests.
- Completed `FFI-050` as a split decision: keep first-release FFI focused on SVG/parse/layout,
  retain feature-gated RaTeX math support, and defer raster functions until a host requires them.
