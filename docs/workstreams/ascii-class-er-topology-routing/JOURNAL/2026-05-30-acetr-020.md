# ACETR-020 Crossing Contract Tests

## Summary

Replaced the public parser-backed class and ER crossing diagnostics with desired crossing output
tests. The expected behavior is to reorder the lower layer so every relationship can be rendered as
a readable adjacent-layer edge.

## Red Validation

- `cargo nextest run -p merman-ascii class_parser_crossing_relationship_layout_reorders_layer_to_render_each_edge` - FAIL, expected red on `crossing class relationship layouts`
- `cargo nextest run -p merman-ascii er_parser_crossing_relationship_layout_reorders_layer_to_render_each_edge` - FAIL, expected red on `crossing ER relationship layouts`

## Handoff

ACETR-030 should make the shared planner reorder lower-layer boxes by parent order, then keep the
existing crossing validation for cases that still cannot be rendered honestly.
