# 2026-05-28 Closeout

## Completed

- Added shared point-list formatting helpers in `svg::parity::util`.
- Added focused utility coverage for point-list formatting.
- Replaced radar graticule and curve polygon point-list loops with the shared helper.
- Preserved existing escaping behavior at radar call sites.

## Evidence

- `cargo nextest run -p merman-render fmt_points`: pass, 1 test.
- `cargo nextest run -p merman-render radar`: pass, 1 test.
- `cargo run -p xtask -- compare-radar-svgs --check-dom --dom-mode parity --dom-decimals 3`: pass.
- `cargo fmt -p merman-render -- --check`: pass.
- `cargo nextest run -p merman-render`: pass, 206 tests.
- `cargo clippy -p merman-render --all-targets -- -D warnings`: pass.

## Next Candidate

Apply the same helper to ER/debug SVG point-list emitters in a separate bounded task. Block has more
shape-specific rendering logic and should come after another small adopter.
