# M15C-050 Sankey 11.15 Baseline Refresh

## Scope

- Checked whether the 24 Sankey `stroke-width` DOM mismatches were stale stored baselines or local
  renderer math drift.
- Refreshed stored Sankey SVG baselines only after the fresh Mermaid 11.15 target compared green.

## Findings

- Stored Sankey baselines failed with 24 `stroke-width` mismatches.
- Fresh Mermaid 11.15 Sankey SVG output matched local output in `parity` mode, so no Sankey renderer
  math change was needed.

## Changes

- Refreshed 33 stored Sankey upstream SVG baselines.
- Updated Sankey alignment documentation from Mermaid 11.12.3 to Mermaid 11.15.0 and listed the
  current stored SVG baseline set.
- Closed M15C-050. Current full parity residuals are Class, Flowchart, and XYChart.

## Evidence

- `cargo run -p xtask -- compare-sankey-svgs --check-dom --dom-mode parity --dom-decimals 3`:
  failed before refresh with 24 `stroke-width` mismatches.
- `cargo run -p xtask -- gen-upstream-svgs --diagram sankey --out target/upstream-svgs-11-15-sankey`:
  passed and generated 33 SVGs.
- `cargo run -p xtask -- compare-svg-xml --check --diagram sankey --upstream-root target/upstream-svgs-11-15-sankey --dom-mode parity --dom-decimals 3`:
  passed.
- `cargo run -p xtask -- gen-upstream-svgs --diagram sankey --out fixtures/upstream-svgs`: passed.
- `cargo run -p xtask -- compare-sankey-svgs --check-dom --dom-mode parity --dom-decimals 3`:
  passed.
- `cargo run -p xtask -- compare-svg-xml --check --diagram sankey --dom-mode parity --dom-decimals 3`:
  passed.
- `cargo nextest run -p merman-render sankey`: passed.
- `cargo run -p xtask -- compare-all-svgs --check-dom --dom-mode parity --dom-decimals 3`:
  failed with current remaining split class=9, flowchart=1, xychart=1.
- `cargo run -p xtask -- check-alignment`: passed.
- `cargo fmt --check`: passed.
- `git diff --check`: passed.
