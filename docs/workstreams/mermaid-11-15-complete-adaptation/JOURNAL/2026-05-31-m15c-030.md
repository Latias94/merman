# M15C-030 Active Baseline Metadata

Date: 2026-05-31
Task: M15C-030
Status: DONE

## Summary

Removed active compare/report metadata that hard-coded Mermaid 11.12.3 as the upstream SVG baseline
version.

## Changes

- Compare report headers now say `pinned Mermaid baseline`.
- `docs/rendering/SVG_CANONICAL_XML.md` now points to `tools/upstreams/REPOS.lock.json`.
- `docs/alignment/PARITY_HARDENING_PLAN.md` now names Mermaid `@11.15.0` as the current baseline
  while preserving historical 11.12.3 inventory labels.

## Evidence

- Green: `cargo fmt --check`
- Green: `git diff --check`
- Green: `cargo nextest run -p xtask`
- Green: `cargo run -p xtask -- check-alignment`
- Green: targeted `rg` check found no active hard-coded Mermaid 11.12.3 labels in compare reports.

## Next

Run M15C-040 for the first 11.15 upstream SVG baseline refresh batch.
