# M15C-060 ER 11.15 Renderer Convergence

## Scope

- Refreshed stored ER upstream SVG baselines to Mermaid 11.15.
- Closed the ER renderer DOM drift exposed by the refresh.
- Re-ran ER and umbrella gates to update the remaining 11.15 failure set.

## Findings

- The previous single ER stored mismatch was stale-baseline-masked renderer drift.
- Refreshing ER baselines exposed 101 ER DOM mismatches.
- Mermaid 11.15 ER output uses the unified renderer envelope: root shadow defs, scoped node/edge
  ids, `data-look="classic"`, centered SVG edge labels, theme gradients, and Rough.js thin
  rectangle dividers for attribute tables.
- The `layout: elk` ER fixture no longer appends `_0` to edge DOM ids in Mermaid 11.15.

## Changes

- Refreshed 101 stored ER SVG baselines.
- Updated ER SVG rendering for 11.15 DOM parity:
  - root drop-shadow defs,
  - scoped node and edge path ids,
  - `data-look="classic"`,
  - no-attribute entity `markdown-node-label`,
  - centered SVG relationship labels,
  - attribute-table thin rectangle dividers,
  - ER root gradients,
  - 11.15 ELK edge DOM ids without `_0`.
- Added regression coverage for ER root gradients and updated existing ER SVG envelope assertions.

## Evidence

- `cargo run -p xtask -- gen-upstream-svgs --diagram er --out fixtures/upstream-svgs`: passed.
- `cargo run -p xtask -- compare-svg-xml --check --diagram er --dom-mode parity --dom-decimals 3`:
  passed.
- `cargo run -p xtask -- compare-er-svgs --check-dom --dom-mode parity --dom-decimals 3`: passed.
- `cargo nextest run -p merman-render er`: passed, 168 tests.
- `cargo nextest run -p merman-render er_svg_forest_theme_renders_root_gradient er_svg_renders_entities_and_relationships er_svg_relationship_labels_follow_flowchart_htmllabels_when_root_unset`:
  passed.
- `cargo fmt --check`: passed.
- `cargo run -p xtask -- compare-all-svgs --check-dom --dom-mode parity --dom-decimals 3`: failed
  only for Class with 14 DOM mismatches.

## Next

Continue M15C-060 with a bounded Class 11.15 convergence slice.
