# M15C-060 Class Convergence

Date: 2026-06-01

## Summary

Closed the Class remainder of M15C-060. Fresh Mermaid 11.15 Class output exposed a broader
renderer-envelope refresh than the stale stored 14-mismatch signal showed. The Class renderer now
matches Mermaid 11.15 structural SVG DOM for the 245 generated upstream SVGs, and stored Class
baselines have been refreshed from that verified target.

`upstream_parser_class_spec` still cannot be treated as an upstream SVG authority because Mermaid
11.15 times out or renders prototype-key artifacts with invalid/missing nodes. It remains excluded
from Class DOM and canonical XML compares.

## Evidence

- `cargo check -p merman-render`: passed.
- `cargo run -p xtask -- compare-svg-xml --check --diagram class --filter namespace --upstream-root target/upstream-svgs-11-15-class --dom-mode parity --dom-decimals 3`: passed.
- `cargo run -p xtask -- compare-svg-xml --check --diagram class --upstream-root target/upstream-svgs-11-15-class --dom-mode parity --dom-decimals 3`: passed, zero mismatches.
- `cargo test -p merman-render --test class_svg_test`: passed, 15 tests.
- `cargo run -p xtask -- compare-class-svgs --check-dom --dom-mode parity --dom-decimals 3`: passed.
- `cargo run -p xtask -- compare-svg-xml --check --diagram class --dom-mode parity --dom-decimals 3`: passed, zero mismatches.
- `cargo run -p xtask -- compare-all-svgs --check-dom --dom-mode parity --dom-decimals 3`: passed.
- `cargo run -p xtask -- check-alignment`: passed.
- `cargo run -p xtask -- verify-generated`: passed after regenerating the stale Flowchart font
  metric entry left by the prior Flowchart 11.15 baseline refresh.
- `cargo run -p xtask -- compare-all-svgs --check-dom --dom-mode parity-root --dom-decimals 3`: failed for root-only residuals outside the Class structural slice, plus one stale expected Flowchart Math policy entry.

## Next

Continue M15C-070. Treat `parity-root` as a root geometry and residual-policy workstream, not as a
Class structural parity blocker.
