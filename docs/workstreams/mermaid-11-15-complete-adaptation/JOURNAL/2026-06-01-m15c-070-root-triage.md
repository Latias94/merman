# M15C-070 Root Triage

Date: 2026-06-01

## Findings

- Full implemented-matrix structural `parity` is green, but `parity-root` is still red.
- A focused Class root check failed with 20 root `style` max-width residuals, so the root set is not
  limited to ER, Flowchart, C4, and Architecture.
- Raw `target/compare/*_report_parity_root.md` counts are flowchart=229, sequence=168,
  architecture=32, class=20, c4=15, timeline=7, mindmap=4, sankey=3, journey=2, and er=4 table
  `dom ok = no` rows.
- `flowchart/upstream_docs_math_flowcharts_001` no longer appears as a root residual after the
  Flowchart 11.15 Math baseline refresh.

## Changes

- Removed the stale accepted root residual policy entry for
  `flowchart/upstream_docs_math_flowcharts_001`.
- Updated M15C-070 docs to describe the current root-only residual matrix.
- Made `xtask report-overrides` read the Mermaid baseline label from
  `tools/upstreams/REPOS.lock.json` instead of printing the old `@11.12.3` label.

## Next

Investigate shared Mermaid 11.15 root viewport behavior before adding any broad fixture-scoped root
override growth. The largest buckets to sample first are Flowchart, Sequence, Class, Architecture,
and C4.
