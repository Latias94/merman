# 2026-05-31 - M15C-040 Sequence Probe

## Scope

- Verified `tools/mermaid-cli` is Mermaid `11.15.0`.
- Used fresh sequence `basic` and `central` upstream SVG probes instead of trusting stored
  11.12-era SVG baselines.
- Kept the full upstream SVG refresh out of this slice.

## Findings

- Fresh 11.15 sequence output scopes base icon and marker ids by SVG id and includes four extra
  sequence arrowhead marker definitions.
- Fresh 11.15 sequence output includes `data-*` metadata on participants, lifelines, messages, and
  notes.
- Stored sequence baselines are stale for marker ids.
- One real local gap remained after using fresh baselines: central connections were parsed as
  actors such as `Alice()` and `()John`.

## Changes

- Added Mermaid 11.15 central-connection parsing/model behavior:
  - visible messages carry `centralConnection` values 59, 60, or 61;
  - type 59/60 internal control messages are inserted;
  - actor ids are normalized to the real participant ids.
- Updated sequence layout and SVG parity rendering for central connection offsets, circles, and
  internal empty activation groups.
- Updated sequence 11.15 marker defs, scoped icon ids, and sequence `data-*` attributes.
- Refreshed only central-connection semantic and layout goldens.

## Evidence

- `cargo run -p xtask -- compare-svg-xml --check --diagram sequence --filter basic --upstream-root target/upstream-svgs-11-15-probe --dom-mode parity --dom-decimals 3`: passed.
- `cargo run -p xtask -- compare-svg-xml --check --diagram sequence --filter central --upstream-root target/upstream-svgs-11-15-central --dom-mode parity --dom-decimals 3`: passed.
- `cargo nextest run -p merman-core sequence`: passed.
- `cargo nextest run -p merman-render sequence`: passed.
- `cargo nextest run -p merman-core fixtures_match_golden_snapshots`: passed.
- `cargo nextest run -p merman-render fixtures_match_layout_golden_snapshots_when_present`: passed.

## Remaining

- M15C-040 is not complete. Timeline still needs fresh 11.15 renderer convergence, and stored
  upstream SVG baseline refreshes remain open.
- Sequence stored upstream SVG baselines still need a deliberate diagram-scoped refresh/check after
  this code fix.

## C4/Journey/Timeline Follow-up Probe

### Scope

- Generated fresh Mermaid 11.15 C4, Journey, and Timeline SVGs under `target/`.
- Kept stored `fixtures/upstream-svgs` unchanged in this slice.

### Findings

- C4 fresh 11.15 output scopes base symbol ids by SVG id and uses updated type-label
  `textLength` constants for `system`, `system_db`, `system_queue`, and `external_person`.
- Journey fresh 11.15 output scopes task-line ids by SVG id.
- Timeline fresh 11.15 output still differs broadly from local output. Representative differences:
  scoped node ids such as `<svg-id>-node-0` versus local `node-undefined`, wrapper class/DOM shape,
  and multiline/tspan output.

### Changes

- Scoped C4 base symbol ids (`computer`, `database`, `clock`) by diagram SVG id.
- Updated C4 11.15 type-label length constants.
- Scoped Journey task-line ids by diagram SVG id.

### Evidence

- `cargo run -p xtask -- compare-svg-xml --check --diagram c4 --upstream-root target/upstream-svgs-11-15-c4 --dom-mode parity --dom-decimals 3`: passed.
- `cargo run -p xtask -- compare-svg-xml --check --diagram journey --upstream-root target/upstream-svgs-11-15-journey --dom-mode parity --dom-decimals 3`: passed.
- `cargo run -p xtask -- compare-svg-xml --check --diagram timeline --upstream-root target/upstream-svgs-11-15-timeline --dom-mode parity --dom-decimals 3`: failed with 90 mismatches.
- `cargo nextest run -p merman-render c4`: passed.
- `cargo nextest run -p merman-render journey`: passed.
- `cargo fmt --check`: passed.
- `git diff --check`: passed.

## Stored Baseline Refresh

### Scope

- Refreshed stored C4 and Journey upstream SVG baselines only after their fresh 11.15 targets were
  green.
- Generated the full fresh Sequence 11.15 corpus as a gate before deciding whether Sequence stored
  baselines can be refreshed.

### Findings

- Full fresh Sequence generation produced 322 SVGs, but full fresh compare still failed with 121
  DOM mismatches. Dominant categories: control-structure metadata/class differences, participant
  type SVG/data differences, note ordering, and text wrapping/tspan differences.
- C4 and Journey are now green against stored upstream SVG fixtures.
- Current full stored-fixture `parity` split after the C4/Journey refresh is sequence=322,
  timeline=91, sankey=24, class=9, flowchart=1, xychart=1.

### Evidence

- `cargo run -p xtask -- gen-upstream-svgs --diagram sequence --out target/upstream-svgs-11-15-sequence`: generated 322 SVGs.
- `cargo run -p xtask -- compare-svg-xml --check --diagram sequence --upstream-root target/upstream-svgs-11-15-sequence --dom-mode parity --dom-decimals 3`: failed with 121 mismatches.
- `cargo run -p xtask -- gen-upstream-svgs --diagram c4 --out fixtures/upstream-svgs`: passed.
- `cargo run -p xtask -- gen-upstream-svgs --diagram journey --out fixtures/upstream-svgs`: passed with upstream Mermaid CLI NaN attribute warnings.
- `cargo run -p xtask -- compare-svg-xml --check --diagram c4 --dom-mode parity --dom-decimals 3`: passed.
- `cargo run -p xtask -- compare-svg-xml --check --diagram journey --dom-mode parity --dom-decimals 3`: passed.
- `cargo run -p xtask -- compare-all-svgs --check-dom --dom-mode parity --dom-decimals 3`: failed; C4 and Journey no longer appear in the failing split.

## Sequence Full Convergence And Stored Refresh

### Scope

- Continued from the full fresh Sequence 11.15 corpus that had 121 DOM mismatches.
- Refreshed stored Sequence upstream SVG baselines only after the full refreshable corpus compared
  green.

### Findings

- The remaining Sequence 11.15 deltas were real renderer/DOM gaps, not only stale marker ids:
  control-structure group metadata, participant type data/classes, queue/database wrapper DOM,
  actor-man glyph ordering/style, section title classes, self-message path attributes, note wrap
  slack, and Sequence math rendering.
- `stress_end_keyword_016` cannot be regenerated by Mermaid 11.15 because upstream rejects `(end)`
  as a participant id. The fixture stays as local parser coverage but is excluded from upstream SVG
  generation/check/compare gates.

### Changes

- Matched Mermaid 11.15 Sequence control-structure, participant actor-type, actor-man, note wrap,
  self-message, and math-renderer DOM behavior.
- Refreshed stored Sequence upstream SVG baselines.
- Added explicit skip handling for the unrefreshable `stress_end_keyword_016` fixture in upstream
  SVG generation/check and both Sequence compare paths.

### Evidence

- `cargo run -p xtask -- gen-upstream-svgs --diagram sequence --out fixtures/upstream-svgs`:
  refreshed stored Sequence SVG baselines.
- `cargo run -p xtask -- compare-sequence-svgs --check-dom --dom-mode parity --dom-decimals 3`:
  passed.
- `cargo run -p xtask -- compare-svg-xml --check --diagram sequence --dom-mode parity --dom-decimals 3`:
  passed with 1 skipped fixture recorded.
- `cargo nextest run -p merman-render sequence`: passed.
- `cargo nextest run -p xtask`: passed.
- `cargo fmt --check`: passed.
- `git diff --check`: passed.
- `cargo run -p xtask -- compare-all-svgs --check-dom --dom-mode parity --dom-decimals 3`:
  failed with current remaining split timeline=91, sankey=24, class=9, flowchart=1, xychart=1.

## Timeline Convergence And Stored Refresh

### Scope

- Continued from the fresh Timeline 11.15 corpus that had 90 DOM mismatches.
- Refreshed stored Timeline upstream SVG baselines only after the fresh 11.15 corpus compared
  green.

### Findings

- The representative Timeline mismatch looked broad in canonical output, but raw SVG inspection
  narrowed the actionable renderer gap to 11.15 scoped node background ids.
- Mermaid 11.15 uses a per-diagram node counter for Timeline background paths:
  `<svg-id>-node-N`. The class remains `node-bkg node-undefined`.

### Changes

- Matched Mermaid 11.15 Timeline scoped node background ids in the parity renderer.
- Refreshed 91 stored Timeline upstream SVG baselines.
- Closed M15C-040. Current full parity residuals are now Sankey, Class, Flowchart, and XYChart.

### Evidence

- `cargo run -p xtask -- compare-svg-xml --check --diagram timeline --filter upstream_docs_timeline_an_example_of_a_timeline_002 --upstream-root target/upstream-svgs-11-15-timeline --dom-mode parity --dom-decimals 3`:
  passed.
- `cargo run -p xtask -- compare-svg-xml --check --diagram timeline --upstream-root target/upstream-svgs-11-15-timeline --dom-mode parity --dom-decimals 3`:
  passed.
- `cargo run -p xtask -- gen-upstream-svgs --diagram timeline --out fixtures/upstream-svgs`:
  shell timed out after 5 minutes, then the original `xtask` process completed and wrote 91 stored
  SVGs.
- `cargo run -p xtask -- compare-timeline-svgs --check-dom --dom-mode parity --dom-decimals 3`:
  passed.
- `cargo run -p xtask -- compare-svg-xml --check --diagram timeline --dom-mode parity --dom-decimals 3`:
  passed.
- `cargo nextest run -p merman-render timeline`: passed.
- `cargo fmt --check`: passed.
- `git diff --check`: passed.
- `cargo run -p xtask -- check-alignment`: passed.
- `cargo run -p xtask -- compare-all-svgs --check-dom --dom-mode parity --dom-decimals 3`:
  failed with current remaining split sankey=24, class=9, flowchart=1, xychart=1.
