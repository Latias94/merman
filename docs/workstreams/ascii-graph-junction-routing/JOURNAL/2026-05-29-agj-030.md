# AGJ-030 Journal

Implemented the routing slice as a direct Rust adaptation of the relevant `mermaid-ascii` Go
algorithm:

- `direction.go`: 3x3 node ports for LR edge start/end choices.
- `mapping_edge.go`: preferred/alternative path selection.
- `arrow.go`: A* grid path search, path segment merging, corner drawing, arrowheads.
- `draw.go`: route-cell junction merging without treating node/subgraph borders as route cells.

Moved four copied fixtures into exact parity:

- `ascii/ampersand_lhs_and_rhs.txt`
- `ascii/backlink_from_top.txt`
- `extended-chars/ampersand_lhs_and_rhs.txt`
- `extended-chars/backlink_from_top.txt`

Validation:

- PASS `cargo fmt --all --check`
- PASS `cargo nextest run -p merman-ascii graph_fixture` (2 passed, 41 skipped)
- PASS `cargo nextest run -p merman-ascii flowchart` (22 passed, 21 skipped)
- PASS `cargo nextest run -p merman-ascii graph::` (6 passed, 37 skipped)

Final AGJ-030 parity count:

- ASCII: 28 exact, 24 named gaps.
- Unicode: 20 exact, 3 named gaps.
- Total: 48 exact graph fixtures.
