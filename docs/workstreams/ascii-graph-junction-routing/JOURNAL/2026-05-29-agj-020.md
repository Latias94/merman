# AGJ-020 Journal

Split `crates/merman-ascii/src/graph/mod.rs` into private renderer modules:

- `charset.rs`: ASCII/Unicode glyph tables.
- `layout.rs`: graph node placement, group layout, and measurement.
- `draw.rs`: render orchestration plus node and group drawing.
- `routing.rs`: edge extents, path selection, line drawing, and labels.

Validation:

- PASS `cargo fmt --all --check`
- PASS `cargo nextest run -p merman-ascii graph_fixture` (2 passed, 41 skipped)
- PASS `cargo nextest run -p merman-ascii graph::` (6 passed, 37 skipped)

Notes:

- The split is behavior-preserving. Junction behavior remains unchanged until AGJ-030.
- `FlowchartV2Model` stays isolated to `adapter.rs`.
