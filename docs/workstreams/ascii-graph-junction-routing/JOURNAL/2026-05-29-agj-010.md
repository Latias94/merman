# AGJ-010 Journal

Opened `ascii-graph-junction-routing` as the durable follow-up to the closed graph routing parity
lane.

Starting exact graph fixture count:

- ASCII: 26 exact, 26 named gaps.
- Unicode: 18 exact, 5 named gaps.
- Total: 44 exact matches.

Planned execution order:

1. Split `graph/mod.rs` into private renderer modules without behavior changes.
2. Add glyph/segment based junction merging for existing LR routes.
3. Move exact crossing fixtures from gaps to allowlist and close with fresh gates.
