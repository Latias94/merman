# 2026-05-28 - Open Workstream

Opened the resvg-safe SVG output pipeline lane after reviewing Zed PR 57644 and the local
`repo-ref/zed/crates/mermaid_render` wrapper.

Key decisions:

- Treat Zed's wrapper as requirements evidence, not implementation source.
- Keep Mermaid parity output as the default.
- Build readable and `resvg`-safe output as explicit pipeline presets.
- Add custom host postprocessors after the built-in boundary is stable.

Initial implementation evidence:

- `foreign_object_label_fallback_svg_text` now splits literal `\n` labels into separate fallback
  overlay lines.
- Focused fallback tests pass.
