# 2026-06-02 - HPD-040 Measurement / Root Bounds Platform

Status: done

Implemented two narrow seams:

- Moved `svg_emitted_bounds` from `svg/parity/state` to `svg/parity`, because it is shared
  root-bounds infrastructure used by State, Architecture, and GitGraph.
- Extracted Sequence note final wrap/measure helpers so layout, root-bounds, and SVG rendering use
  the same Mermaid source-backed final wrapping rule.

Verification:

- `cargo fmt --all`
- `cargo test -p merman-render svg_emitted_bounds --lib`
- `cargo test -p merman-render sequence_long_leftof_notes_keep_mermaid_11_15_note_width --test sequence_svg_test`
- `cargo test -p merman-render architecture_text_constants_match_mermaid --lib`
- `cargo run -p xtask -- report-overrides --check-no-growth`

Residual kept open:

- `cargo test -p merman-render sequence_long_leftof_notes_keep_mermaid_11_15_root_width --test sequence_svg_test`
  still fails. Deterministic local root width is `570px` vs upstream `566px`; headless vendored
  report is `585px` vs upstream `566px`.
- This is retained as a measurement/root residual. HPD-040 intentionally did not add an override or
  fixture-specific width adjustment.

Next:

- Start HPD-050 with Architecture residual source audit, focusing first on input-model and
  bounds-feeding seams before changing `manatee` solver behavior.
