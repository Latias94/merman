# HPD-090 Closeout

HPD-090 is closed.

Outcome:

- The Mermaid 11.15 baseline preparation queue is complete.
- Current-facing baseline surfaces for Info, Error, and `xtask` live labels now use pinned baseline
  truth.
- Stored upstream SVG drift was classified by family instead of refreshed all at once.
- Requirement, Block, Gantt, Kanban, Mindmap, Radar, Class, Timeline, and Flowchart HTML demo
  KaTeX stale sets are refreshed or point-refreshed.
- Class and Timeline required source-backed local fixes; Flowchart HTML demo KaTeX was a pure
  stored-SVG refresh.
- No broad or narrow stale stored-SVG set remains known.
- No broad official fixture import is indicated by the current inventory.

Closeout verification:

- `cargo fmt --check` - passed.
- `cargo nextest run -p merman-render --test layout_snapshots_test fixtures_match_layout_golden_snapshots_when_present` -
  passed, `1` test run.
- `cargo nextest run -p merman --features render --test resvg_safe_fixture_smoke boundary_fixtures_render_headless_resvg_safe` -
  passed, `1` test run and `5` skipped.
- `cargo run -p xtask -- compare-all-svgs --check-dom --dom-mode parity --dom-decimals 3` -
  passed for the implemented matrix.
- JSON/JSONL validation for `TASKS.jsonl`, `CONTEXT.jsonl`, `CAMPAIGNS.jsonl`, and
  `WORKSTREAM.json` - passed.
- `git diff --check` - passed; Git only reported the existing JSONL LF-to-CRLF working copy
  warnings.

Gate note:

- `check-upstream-svgs --diagram all` was not rerun. HPD-090 already recorded that the all-family
  upstream check is too coarse on Windows for this corpus, so closeout relies on per-family
  inventory logs plus focused stale-family/fixture refresh checks.

Next:

- Continue HPD-080 only from a fresh failing renderability gate, source-backed emitted-surface gap,
  or concrete consumer report.
- Otherwise resume HPD-050 source-backed Architecture/Dagre/Graphlib audits.
