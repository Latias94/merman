# ACEMPR-030 Layered Parallel Lane Offsets

Date: 2026-05-30
Owner: codex
Status: DONE

## Summary

Allowed duplicate endpoint pairs through layered level assignment and offset duplicate lanes during
class/ER drawing. Drawing now paints center routes before offset parallel lanes so later ordinary
routes do not overwrite duplicate-lane text.

The output remains compact and terminal-native. Rich label collision avoidance is still dense
routing scope.

## Evidence

- `cargo nextest run -p merman-ascii class`: PASS.
- `cargo nextest run -p merman-ascii er`: PASS.
- `cargo clippy -p merman-ascii --all-targets -- -D warnings`: PASS.
- `cargo fmt --all --check`: PASS.
- `git diff --check -- crates/merman-ascii/src/relation_graph.rs crates/merman-ascii/src/class/render.rs crates/merman-ascii/src/er/render.rs crates/merman-ascii/tests/class_model.rs crates/merman-ascii/tests/er_model.rs docs/workstreams/ascii-class-er-mixed-parallel-routing`: PASS.

## Next

ACEMPR-040 should update public support docs, run full package gates, and close the lane.
