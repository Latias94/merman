# AFMS-020 - Contract Tests

Date: 2026-05-30
Task: AFMS-020
Status: DONE

## Summary

Added two public rendering contracts for multiline flowchart subgraph titles:

- `flowchart_parser_multiline_subgraph_title_renders_centered_rows`
- `render_flowchart_renders_model_multiline_subgraph_titles`

## Red Evidence

- `cargo nextest run -p merman-ascii flowchart_parser_multiline_subgraph_title_renders_centered_rows`
  fails because the current renderer outputs `Line<br>Two` as one raw title row.
- `cargo nextest run -p merman-ascii render_flowchart_renders_model_multiline_subgraph_titles`
  fails because the current adapter returns `multiline subgraph labels` for real newline titles.

## Next

AFMS-030 should reuse `GraphLabel` for group title width/height and drawing, while keeping parser
and core untouched.
