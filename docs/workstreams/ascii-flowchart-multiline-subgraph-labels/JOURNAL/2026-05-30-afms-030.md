# AFMS-030 - Layout And Drawing

Date: 2026-05-30
Task: AFMS-030
Status: DONE

## Summary

Implemented multiline flowchart subgraph title rendering in `merman-ascii`.

## Changes

- Reused `GraphLabel` for subgraph title width and title-space calculations.
- Drew group titles line by line, centered within the group box.
- Kept BT/RL transformed group titles readable by preserving multiline title order after output
  transforms.
- Relaxed the adapter so direct-model subgraph titles with real newlines render, while invalid
  member ids with line breaks still return an explicit diagnostic.

## Verification

- `cargo nextest run -p merman-ascii flowchart_parser_multiline_subgraph_title_renders_centered_rows render_flowchart_renders_model_multiline_subgraph_titles` - PASS
- `cargo nextest run -p merman-ascii flowchart` - PASS
- `cargo nextest run -p merman-ascii graph_fixture` - PASS
