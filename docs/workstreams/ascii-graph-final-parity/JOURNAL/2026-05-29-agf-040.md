# AGF-040 Journal

Date: 2026-05-29
Task: AGF-040

## Change

- Matched Mermaid-ascii subgraph title width and title overlay behavior.
- Replaced unconditional group offsets with dynamic negative-coordinate offsets.
- Added TD grid branching for multiple child nodes.
- Added subgraph external incoming padding overhead.
- Added bent TD routing for cross-column downward edges.
- Added LR external/subgraph root separation.
- Added nested subgraph support by tracking group ids and allowing group bounds to include child
  group bounds.
- Moved all remaining ASCII subgraph fixtures into the graph fixture allowlist.

## Validation

- `cargo fmt --all --check`
- `cargo nextest run -p merman-ascii graph_fixture`
- `cargo nextest run -p merman-ascii flowchart`
- `git diff --check`

## Notes

- Current graph fixture parity is 75 exact matches: 52 ASCII and 23 Unicode.
- `GRAPH_FIXTURE_GAPS.md` now records no named graph fixture gaps.
