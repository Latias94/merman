# AGF-050 Journal

Date: 2026-05-29
Task: AGF-050

## Change

- Updated flowchart support docs for multiline node labels, nested/external subgraphs, TD branch
  layout, and the final graph fixture count.
- Updated the changelog Unreleased section with completed copied graph fixture parity.
- Closed the workstream after broad gates.

## Validation

- `cargo fmt --all --check`
- `cargo nextest run -p merman-ascii`
- `cargo nextest run -p merman --features ascii`
- `cargo nextest run -p merman-cli --features ascii`
- `cargo clippy -p merman-ascii -p merman --features ascii --all-targets -- -D warnings`
- `git diff --check`

## Notes

- Latest release tag checked for changelog context: `v0.6.0`.
- Changelog update stayed in `Unreleased`; no release version was finalized.
