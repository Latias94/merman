# ACEG-020 Contract Tracer Tests

Date: 2026-05-30
Status: DONE

## Summary

Added parser-backed tests for the current explicit diagnostics that ACEG-030 must replace with a
shared layout boundary:

- class multiple relationships still return `multiple class relationships`,
- ER relationship layouts with unrelated entities still return
  `ER relationship layouts with unrelated entities`.

No production renderer code changed.

## Validation

- PASS: `cargo nextest run -p merman-ascii class` - 12 tests
- PASS: `cargo nextest run -p merman-ascii er` - 77 tests

## Handoff

ACEG-030 can now refactor toward a shared terminal relationship graph placement seam with both
class and ER public-path diagnostics locked.
