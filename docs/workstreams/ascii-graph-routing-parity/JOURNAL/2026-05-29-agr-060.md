# AGR-060 Journal

Date: 2026-05-29

## Closeout

Closed the ASCII graph routing parity lane for the current time box.

Shipped slices:

- Graph model/adapter boundary split.
- Copied `mermaid-ascii` graph fixture harness with allowlist and named gaps.
- LR grid placement for roots and child levels.
- Basic branch, multi-root, fan-out, fan-in, self-loop, and same-row back-edge routing.
- Upstream-style simple subgraph title rows and empty-subgraph offset handling.
- Support docs and changelog updates.

Final fixture state:

- 44 exact graph fixture matches.
- ASCII: 26 exact matches, 26 named gaps.
- Unicode: 18 exact matches, 5 named gaps.

## Verification

- PASS `cargo fmt --all --check`
- PASS `cargo nextest run -p merman-ascii` (43 passed)
- PASS `cargo nextest run -p merman --features ascii` (3 passed)
- PASS `cargo nextest run -p merman-cli --features ascii` (10 passed)
- PASS `cargo clippy -p merman-ascii -p merman --features ascii --all-targets -- -D warnings`
- PASS `git diff --check`

## Follow-Ons

- Crossing/junction merging.
- Duplicate and bidirectional label separation.
- Padding directive parity for copied fixtures.
- TD back-edge labels.
- Complex nested/external-edge subgraphs.
