# AGR-020 Journal

Date: 2026-05-29

## Change

Added an integration fixture harness for copied `mermaid-ascii` graph fixtures:

- `tests/graph_fixture.rs` renders allowlisted graph fixtures through `merman-core` and
  `merman-ascii`.
- The harness records 13 current exact matches: 7 ASCII and 6 Unicode.
- Every remaining copied graph fixture is explicitly named in `GRAPH_FIXTURE_GAPS`.
- The coverage test fails if a copied `ascii` or `extended-chars` graph fixture is neither
  allowlisted nor named as a gap.
- `GRAPH_FIXTURE_GAPS.md` mirrors the readable planning inventory.

## Verification

- PASS `cargo fmt --all --check`
- PASS `cargo nextest run -p merman-ascii graph_fixture` (2 passed, 41 skipped)
- PASS `cargo nextest run -p merman-ascii flowchart` (22 passed, 21 skipped)

## Next

AGR-030: implement branch/multi-root routing against reference fixtures and move exact matches from
gap inventory to allowlist.
