# AGR-040 Journal

Date: 2026-05-29

## Change

Added a focused back-edge and self-reference routing slice:

- LR self-loops draw a right-side loop, expand the canvas, and use Unicode junctions where needed.
- Self-loops render after non-self edges so `A --> A & B` keeps the shared junction.
- Same-row right-to-left edges route below the row and re-enter the target with an upward arrow.
- The graph fixture allowlist increased from 31 to 37 exact matches.

## Verification

- PASS `cargo fmt --all --check`
- PASS `cargo nextest run -p merman-ascii graph_fixture` (2 passed, 41 skipped)
- PASS `cargo nextest run -p merman-ascii flowchart` (22 passed, 21 skipped)
- PASS `cargo nextest run -p merman-ascii` (43 passed)
- PASS `cargo clippy -p merman-ascii --all-targets -- -D warnings`

## Remaining Gaps

- Crossing/junction merging for combined branch fixtures.
- TD back-edge and bidirectional label separation.
- Padding directive handling in copied reference fixtures.
- Subgraph exact layout parity.
