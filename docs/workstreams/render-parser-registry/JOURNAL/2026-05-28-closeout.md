# 2026-05-28 Closeout

## Completed

- Added `RenderSemanticParser` and `RenderDiagramRegistry`.
- Registered Mermaid 11.12.2 typed render parsers and aliases.
- Added render registry accessors to `Engine`.
- Replaced the large `Engine::parse_render_semantic_model` match with registry lookup plus JSON
  fallback.
- Added focused tests for typed alias lookup and custom JSON fallback.

## Evidence

- `cargo nextest run -p merman-core render_parser_registry`: pass, 2 tests.
- `cargo fmt -p merman-core -p merman-render -- --check`: pass.
- `cargo nextest run -p merman-core`: pass, 525 tests.
- `cargo nextest run -p merman-render`: pass, 205 tests.
- `cargo clippy -p merman-core --all-targets -- -D warnings`: pass.
- `cargo clippy -p merman-render --all-targets -- -D warnings`: pass.

## Follow-On

Macro/table generation is not needed yet. Revisit only if new diagram work keeps requiring repeated
parser registration boilerplate.
