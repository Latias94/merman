[
  {
    "op": "set",
    "path": ["class", "padding"],
    "value": 12,
    "reason": "Local Class parity uses Mermaid's observed class node padding baseline; upstream schema still lists 5."
  },
  {
    "op": "set",
    "path": ["flowchart", "htmlLabels"],
    "value": true,
    "reason": "Normalize the deprecated nullable flowchart htmlLabels schema default to the effective Mermaid baseline used by local renderers."
  },
  {
    "op": "set",
    "path": ["sankey", "nodeColors"],
    "value": {},
    "reason": "Mermaid defaultConfig.ts exposes this key as undefined for configKeys; local JSON uses an empty object as the equivalent default map."
  },
  {
    "op": "set",
    "path": ["sankey", "useMaxWidth"],
    "value": true,
    "reason": "Pinned Mermaid Sankey baselines render the default SVG with width=100% and max-width styling, so the generated default config must keep the runtime max-width default enabled."
  },
  {
    "op": "set",
    "path": ["architecture", "seed"],
    "value": 1,
    "reason": "Local Architecture FCoSE parity keeps the Mermaid 11.15 deterministic seed default proven by architecture specs and local tests."
  },
  {
    "op": "set",
    "path": ["pie", "donutHole"],
    "value": 0,
    "reason": "Pie 11.15 parity implements donutHole behavior; keep the runtime default in generated JSON even though the pinned schema omits it."
  },
  {
    "op": "set",
    "path": ["pie", "highlightSlice"],
    "value": "",
    "reason": "Pie 11.15 parity implements highlightSlice behavior; keep the runtime default in generated JSON even though the pinned schema omits it."
  },
  {
    "op": "set",
    "path": ["pie", "legendPosition"],
    "value": "right",
    "reason": "Pie 11.15 parity implements legendPosition behavior; keep the runtime default in generated JSON even though the pinned schema omits it."
  },
  {
    "op": "remove",
    "path": ["wardley-beta"],
    "reason": "Wardley config exists in the pinned Mermaid 11.15 schema, but the Wardley family is not admitted in the local inventory."
  },
  {
    "op": "remove",
    "path": ["cynefin"],
    "reason": "Out-of-scope 11.15 diagram family for the current baseline."
  },
  {
    "op": "remove",
    "path": ["railroad"],
    "reason": "Out-of-scope 11.15 diagram family for the current baseline."
  }
]
