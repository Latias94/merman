# ASCII Gap Registry

Status: Active evidence registry
Last updated: 2026-08-14

This registry maps remaining ASCII renderer gaps to owning modules, dependencies, and validation
gates. It does not replace the support matrices:

- `FLOWCHART_SUPPORT.md` remains the shipped flowchart support boundary.
- `SEQUENCE_SUPPORT.md` remains the shipped sequence support boundary.
- `STATE_SUPPORT.md` remains the shipped state support boundary.
- `GRAPH_FIXTURE_GAPS.md` and `SEQUENCE_FIXTURE_GAPS.md` remain copied-fixture gap evidence.

Use this file as the first planning context for new ASCII workstreams.

## Registry

| Gap ID | Capability gap | Owning module | Dependencies | Validation gate | Source |
| --- | --- | --- | --- | --- | --- |
| A-TEXT-010 | CJK/emoji multi-cell placement coverage discipline. | `text::StyledLine`, `canvas`, family label/layout modules | Primitive terminal-cell ownership now covers wide glyph continuation cells, final-column rejection, overwrite behavior, and ANSI/HTML style ownership. Semantic family coverage exists for Flowchart CJK/emoji node and edge labels, Sequence CJK message wrapping/alignment, Class CJK/emoji members plus relation-summary labels, ER CJK/emoji attributes plus relation-summary labels, and XYChart CJK categories. Remaining work is admission discipline for future renderer families and more complex grapheme clusters; do not adopt upstream multibyte LR spacing as a byte oracle. | `cargo nextest run -p merman-ascii wide`; `cargo nextest run -p merman-ascii emoji`; `cargo nextest run -p merman-ascii cjk`; `cargo nextest run -p merman-ascii` | `FLOWCHART_SUPPORT.md`, `SEQUENCE_SUPPORT.md`, `V1_MERMAID_ASCII_COVERAGE.md`, `ASCII_REFERENCE_COMPARISON.md` |
| A-STYLE-010 | Background/fill style semantics for ANSI/HTML output. | `color`, `canvas`, `text::StyledLine`, shared CSS color parsing, graph/state/sequence adapters | Flowchart, state, and sequence node/group/box/rect backgrounds now flow through the renderer while preserving plain output. Remaining work is limited to broader CSS color forms that terminals cannot represent faithfully, especially alpha blending. | `cargo nextest run -p merman-ascii color`; `cargo nextest run -p merman-ascii flowchart sequence state` | ADR 0067; `FLOWCHART_SUPPORT.md`, `SEQUENCE_SUPPORT.md`, `STATE_SUPPORT.md`; 2026-06-23 sequence background closeout |
| A-GRAPH-010 | Flowchart subgraph direction overrides. | `graph::layout`, `graph::routing`, `graph::draw` | Shipped behavior preserves explicit local directions across boundary edges and applies Mermaid 11.16.1's perpendicular default to isolated implicit subgraphs, including nested and reverse-root cases. Keep adding explicit cases only when a concrete Mermaid/parser example proves a remaining gap. | `cargo nextest run -p merman-ascii flowchart`; `cargo nextest run -p merman-ascii graph_fixture` | `FLOWCHART_SUPPORT.md`; 2026-06-24 nested override closeout; 2026-08-11 pinned default-axis alignment |
| A-GRAPH-020 | Remaining useful uncommon flowchart shapes, Markdown/HTML labels, links, and callbacks. The canonical registry now gives every pinned Mermaid 11.16.1 shape name an explicit diagrammatic, approximate, or unsupported disposition; common decorated process/document, fork/join, choice, start/end, flag, paper-tape, and text projections are shipped. | `graph::shape`, `graph::label`, `graph::draw`, `graph::adapter` | Add source-backed terminal geometry one family at a time. Keep icons, images, cross-family boxes, and browser-only metadata explicitly unsupported rather than collapsing them to rectangles. | `cargo nextest run -p merman-ascii flowchart`; `cargo nextest run -p merman-ascii shape_registry`; feature-specific parser/model tests | `FLOWCHART_SUPPORT.md`; 2026-08-09 canonical shape disposition closeout |
| A-GRAPH-030 | Broader graph route-plan coverage beyond top-down direct routes. | `graph::routing::plan`, `graph::routing`, `graph::routing::path` | Migrate one route family at a time; keep route-plan tests independent of full snapshots. Boundary grid-path labels now carry explicit planner-owned placements, attach to planner-selected vertical transit lanes in the shipped `LR`-inside-`TD` subset, and contribute their planned overlay extent to canvas sizing. Same-rank top-down edges now use natural horizontal ports in either direction when the span is clear and any label fits without covering an arrowhead; they reuse the shared bottom lane when another node blocks that span or the direct label would cover an arrowhead. Remaining work is broader route-family policy, not post-render label guessing. | `cargo nextest run -p merman-ascii edge_route_selects_top_down`; `cargo nextest run -p merman-ascii flowchart` | `ascii-architecture-deepening` AAD-030; PR #47 |
| A-GRAPH-040 | Copied graph route and layout shapes that intentionally differ after Dagre and compound-scene adoption. | `graph::layout`, `graph::routing`, `graph::topology`; copied fixture inventory | Keep copied fixture bytes immutable. The executable corpus derives 40 exact matches and 39 named deterministic differences; every gap must still render, while parser-backed semantic tests own ranks, endpoints, labels, directions, and compound boundaries. Five implicit-subgraph fixtures intentionally follow Mermaid 11.16.1's perpendicular default instead of the copied renderer's inherited root axis. Do not tune source-backed Dagre or occupancy behavior merely to recreate the narrow renderer's path shape. | `cargo nextest run -p merman-ascii graph_fixture`; `cargo nextest run -p merman-ascii flowchart` | `tests/testdata/mermaid-ascii/GRAPH_FIXTURE_GAPS.md`; `FLOWCHART_SUPPORT.md` |
| A-GRAPH-050 | Ordinary Flowchart node-label wrapping for terminal readability. | `options`, `graph::adapter`, `graph::label`, `graph::layout::grid` | Shipped behavior plans ordinary Flowchart labels at a configurable non-zero terminal display-cell width (default `40`) before shape sizing and routing. The same grapheme-safe plan materializes rows after aggregate grid admission; explicit breaks remain hard breaks, State is unaffected, and SVG pixel widths are not quantized into columns. | `cargo nextest run -p merman-ascii --test flowchart_model -j1`; binding/Web option contract tests | Issue #53; `FLOWCHART_SUPPORT.md`; `tests/testdata/local-semantic/flowchart/issue_53_long_node_labels.mmd` |
| A-SEQ-020 | Empty sequence boxes. | `sequence::boxes`, `sequence::render` | Empty background regions now render as diagram-wide terminal regions when the typed model provides no actor anchors. Sequence box layout now keeps explicit inner padding around nested participant rows and control frames, and all-participant boxes widen around dynamic lifecycle/control content so borders do not merge with inner frame, participant, or lifecycle rows. Keep this policy stable: do not invent hidden participants or infer actor ownership from surrounding messages. | `cargo nextest run -p merman-ascii sequence_empty_boxes_render_as_diagram_wide_regions sequence_box_keeps_inner_padding_around_participants_and_frames sequence_box_with_lifecycle_and_mirror_keeps_boundaries`; `cargo nextest run -p merman-ascii sequence` | `SEQUENCE_SUPPORT.md` |
| A-SEQ-030 | SVG-specific actor shape fidelity beyond terminal participant boxes. | `sequence::model`, `sequence::validate`, `sequence::render` | Actor declarations and extended actor types render as participant boxes. Current actor links and presentation properties are accepted as SVG metadata and intentionally omitted from ASCII; their policy is closed and tested. Only a future typed actor presentation variant needs a new explicit terminal rule. | `cargo nextest run -p merman-ascii sequence_actor_links_do_not_block_ascii_rendering sequence_actor_properties_are_accepted_as_omitted_metadata sequence_extended_actor_types_render_as_participant_boxes`; `cargo nextest run -p merman-ascii sequence` | `SEQUENCE_SUPPORT.md` |
| <a id="a-state-010"></a>A-STATE-010 | Remaining state presentation metadata and future state shape variants. | `state::adapter`, `graph::model`, `graph::draw`, `graph::routing`, `color` | Start/end, fork/join, choice, notes, links, composite boundary transitions, divider/concurrency regions, and ANSI/HTML node/group backgrounds now have documented terminal behavior. Add future Mermaid state semantics only when the adapter can either render them through graph/text primitives or preserve them as precise unsupported features. | `cargo nextest run -p merman-ascii state`; `cargo nextest run -p merman-ascii` | `STATE_SUPPORT.md`; `docs/plans/2026-06-23-003-refactor-ascii-capability-parity-state-plan.md` |
| <a id="a-classer-010"></a>A-CLASSER-010 | Dense, cyclic, and more complex class/ER relation graph layouts. | `relation_graph`, `class::render`, `er::render` | Shared relation labels now cover multiline class/ER relationship labels across vertical stacks, self-relation loops, same-endpoint lanes, bidirectional same-pair lanes, and layered overlays. Parallel same-endpoint Class/ER self-relations share one rendered box and stack compact right-side loop rows instead of duplicating the endpoint box. `relation_components` splits independent relation subgraphs before layered planning so unrelated pairs do not force each other into readability fallback. Class now plans simple sibling-namespace, namespace-to-root, and nested-sibling relations at their least common scope, routes them through immediate namespace facades, and preserves authored leaf identity with byte-length-framed member fields. `LayeredRelationScene` owns layered placement, bounded barycenter sweeps, lane ordering, route-first/overlay-last drawing, direction-aware labels, and lane width budgeting. A strict planar K2×2 component—four nodes, four unique endpoint pairs, and one four-edge cycle—has a targeted bounded cycle layout. Its Class/ER parser tests retain all four relationships diagrammatically; shared tests prove direction-independent placement, four disjoint routes, declaration-reorder stability, exact/N−1 work admission, and speculative-work/document-cell ownership when an overlay collision selects fallback. This is not a general bounded-topology claim. When other bounded ordering cannot produce a readable deterministic drawing, when parallel lane ports cannot land on both endpoint box faces, or when a route/overlay would collide with a box, Class/ER render an explicit structured relation summary that preserves endpoints, connectors, and multiline labels. Dense or colliding cross-namespace Class scenes retain the same lossless fallback. Summary reasons distinguish crossing, route collision, and overlay collision; port-fit failures use the route-collision reason. Resource limits are separate hard errors and never select this fallback. Remaining work is true topology depth beyond the strict K2×2 path: decide when another dense case deserves a routed grid rather than the honest summary adapter. | `cargo nextest run -p merman-ascii class er`; `cargo nextest run -p merman-ascii namespace_scope_index_admits_exact_work_and_rolls_back_n_minus_one class_parser_sibling_namespace_relation_routes_through_facades class_parser_nested_sibling_relation_routes_at_nearest_common_scope class_parser_right_left_namespace_to_root_relation_preserves_semantics class_parser_cross_namespace_collision_keeps_lossless_summary class_parser_parallel_relationship_layout_uses_lossless_summary_when_ports_do_not_fit er_parser_parallel_relationship_layout_uses_lossless_summary_when_ports_do_not_fit class_parser_self_relation_renders_single_box_with_loop er_parser_self_relationship_renders_single_box_with_loop class_parser_parallel_self_relations_share_single_box_loop er_parser_parallel_self_relationships_share_single_box_loop class_parser_multi_parent_relationship_layout_uses_barycenter_order er_parser_multi_parent_relationship_layout_uses_barycenter_order class_parser_child_weighted_parent_order_keeps_readable_layout_routed er_parser_child_weighted_parent_order_keeps_readable_layout_routed class_parser_four_layer_relation_layout_uses_iterative_sweep_order er_parser_four_layer_relationship_layout_uses_iterative_sweep_order relation_components_split_disconnected_relation_subgraphs class_parser_independent_relation_pairs_render_without_shared_summary_state er_parser_independent_relationship_pairs_render_without_shared_summary_state class_parser_k2_2_relationships_use_a_bounded_planar_layout er_parser_k2_2_relationships_use_a_bounded_planar_layout strict_k2_2_uses_a_direction_independent_cycle_with_four_disjoint_routes strict_k2_2_geometry_is_stable_when_declarations_are_reordered strict_k2_2_route_batch_admits_exact_work_and_rolls_back_n_minus_one strict_k2_2_overlay_collision_keeps_speculative_work_but_discards_document_cells render_layered_relation_component_propagates_grid_resource_errors render_layered_relation_component_uses_summary_when_route_path_overlaps_box render_layered_relation_component_uses_summary_when_overlay_overlaps_box` | README Supported Families; `ASCII_REFERENCE_COMPARISON.md`; class/ER tests; 2026-08-13 scoped namespace-facade and strict planar K2×2 routing closeout |
| <a id="a-xy-010"></a>A-XY-010 | Dense cross-series XYChart collision ownership. | `xychart::plot`, `xychart::render`; `merman-core::diagrams::xychart`; `merman-render::xychart` | A shared `TerminalChartPlan` consumes model-owned x/y samples, titles, and point labels; parser-produced x coordinates derive from the typed axis/category domain and sample order rather than independent per-point source pairs. It preserves band/linear and reversed/degenerate ranges, maps linear x coordinates, groups vertical and horizontal bars into recoverable lanes, splits line paths at missing samples, connects horizontal line paths, resolves each line series through a direction-mask topology into ASCII segments or Unicode corners/tees/crossings, separates shortest-roundtrip data formatting from scale-aware ticks, and discloses clipped/missing/orphan/duplicate/normalized/alignment-changing/quantized-collision facts through injective byte-length-framed fields. Invalid direct-model orientations and Band y-axes reject rather than changing projection semantics, while accessibility title/description metadata is intentionally omitted. Expanded plot plus temporary topology extents are budgeted before allocation. Remaining work is cross-series same-cell glyph ownership after terminal quantization; exact disclosure prevents silent data loss. | `cargo nextest run -p merman-core xychart`; `cargo nextest run -p merman-ascii xychart`; `cargo nextest run -p merman-ascii`; `cargo nextest run -p merman-render --test xychart_svg_test` | README XYChart contract; `tests/xychart_model.rs`; 2026-08-14 projection-identity and document-admission closeout |
| <a id="a-admission-010"></a>A-ADMISSION-010 | Railroad, Requirement, Ishikawa, and Quadrant do not yet have an admitted spatial terminal projection. | Future family-owned adapters over the existing terminal mechanisms | The tracked R34 report retains the one-time small/typical/dense prototype decision at 80/100/120 columns. The rejected prototype renderer was removed at closeout; the standing test now locks only the public unsupported capability, catalog, fixture parsing, and dispatcher contract. Reopen one family only with a complete vertical slice; a list, outline, edge dump, or point table alone cannot satisfy Diagrammatic admission. | `cargo nextest run -p merman-ascii --test candidate_admission`; full capability/public-catalog gates before any promotion | `docs/rendering/ASCII_PHASE_GATE_REPORT.md` |
| A-ZEN-010 | Family-owned ZenUML terminal projection. | New `zenuml` adapter over `ZenumlDiagramRenderModel` plus shared terminal primitives | Do not translate ZenUML through Sequence. Admit participants, messages, groups, creation, returns, references, and every fragment kind explicitly before advertising ASCII support. | `cargo nextest run -p merman-ascii zenuml`; capability and support-matrix tests | ADR 0061, ADR 0065, `docs/alignment/ZENUML_UPSTREAM_TEST_COVERAGE.md` |
| A-FAMILY-010 | Additional Mermaid families after state diagrams. | New family adapters plus shared graph/text modules | TreeView, Mindmap, Timeline, Gantt, Journey, Kanban, Packet, and GitGraph have structured-text projections; keep ZenUML and the remaining unsupported families explicit and honest until each owns a typed adapter. | `cargo nextest run -p merman-ascii render_model`; family-specific tests | ADR 0014, ADR 0065, `STATE_SUPPORT.md` |

## Closeout Discipline

When a gap is implemented:

- update the relevant support matrix first;
- update `src/capability.rs` when the public support level, limits, or evidence story changes;
- add or update tests named in the validation gate;
- record the closing workstream or commit in this registry;
- remove the gap only when shipped behavior is documented elsewhere.
