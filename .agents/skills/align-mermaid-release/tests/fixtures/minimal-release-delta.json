{
  "release": {
    "selected_tag": "fixture-release",
    "selected_commit": "fixture-commit"
  },
  "companions": [
    {
      "package": "@fixture/companion",
      "oracle_version": "1.4.0",
      "declared_range": "^1.4.0",
      "candidate_version": "2.0.0",
      "range_relation": "outside-range-major",
      "disposition": "separately-scoped",
      "capability_owner": "fixture-companion-port"
    }
  ],
  "capabilities": [
    {
      "id": "fixture-built-in-diagram",
      "kind": "built-in-diagram",
      "change": "added",
      "capability_owner": "fixture-family",
      "evidence": {
        "parser": "fixture parser and recovery corpus",
        "editor": "fixture editor facts and LSP projection",
        "render": "fixture layout and SVG parity corpus",
        "playground": "fixture catalog and browser smoke"
      }
    }
  ],
  "feature_decision": {
    "outcome": "no-split",
    "reason": "The built-in fixture uses the existing registry and adds no optional dependency.",
    "evidence": {
      "dependency_graph": "unchanged dependency graph",
      "targets": "existing native and WASM targets",
      "licenses": "no new license obligations",
      "clean_build": "no new cache-independent build cost",
      "artifact_size": "no new browser or Typst artifact cost",
      "package_surfaces": "existing public package presets"
    }
  },
  "command_sequence": [
    "cargo run -p xtask -- verify-mermaid-reference --materialized",
    "cargo run -p xtask -- verify-playground-example-catalog",
    "cargo run -p xtask -- verify-web-diagram-catalog",
    "cargo run -p xtask -- check-alignment",
    "cargo run -p xtask -- verify-lalrpop-parsers",
    "cargo run -p xtask -- verify --strict",
    "cargo run -p xtask -- wasm-size-matrix --budget-file docs/release/WASM_SIZE_BUDGETS.json"
  ]
}
